firesignal(game:GetService("ReplicatedStorage").EntityInfo.Caption.OnClientEvent, 'Ty For Using Crucifix Hub Made By Sus! OvO',true,3)

--[[

    NOTE: Keep in mind that these are "fake" achievements and will NOT give you an in-game badge.

]]--

local Achievements = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Custom%20Achievements/Source.lua"))()

-- Creates and displays your custom achievement
Achievements.Get({
    Title = "「✝」-~{Crucifix-Hub}~-",
    Desc = "Thanks For Using 「✝」-~{Crucifix-Hub}~-!",
    Reason = "~{☆Made By Sus☆}~",
    Image = "https://tr.rbxcdn.com/8fd01aed533ef0d236d7a9cbaf0caada/420/420/Decal/Png",
})

local OrionLib = loadstring(game:HttpGet(('https://raw.githubusercontent.com/shlexware/Orion/main/source')))()

local Window = OrionLib:MakeWindow({Name = "☆♡Crucifix♤◇♤Hub♡☆", Icon = "rbxassetid://7734068321", IntroIcon = "rbxassetid://7734068321", IntroText = "(。O w O。)"})

--[[
Name = <string> - The name of the UI.
HidePremium = <bool> - Whether or not the user details shows Premium status or not.
SaveConfig = <bool> - Toggles the config saving in the UI.
ConfigFolder = <string> - The name of the folder where the configs are saved.
IntroEnabled = <bool> - Whether or not to show the intro animation.
IntroText = <string> - Text to show in the intro animation.
IntroIcon = <string> - URL to the image you want to use in the intro animation.
Icon = <string> - URL to the image you want displayed on the window.
CloseCallback = <function> - Function to execute when the window is closed.
]]

local Tab = Window:MakeTab({
	Name = "「✝」+[Stuff]+",
	Icon = "rbxassetid://4483345998",
	PremiumOnly = false
})

--[[
Name = <string> - The name of the tab.
Icon = <string> - The icon of the tab.
PremiumOnly = <bool> - Makes the tab accessible to Sirus Premium users only.
]]

local Section = Tab:AddSection({
	Name = "+[Stuff]+"
})

--[[
Name = <string> - The name of the section.
]]

Tab:AddLabel("Keyboard")

Tab:AddButton({
	Name = "Keyboard",
	Callback = function()
      		loadstring(game:HttpGet("https://raw.githubusercontent.com/advxzivhsjjdhxhsidifvsh/mobkeyboard/main/main.txt", true))()
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddLabel("Press Q To Make Crucifix On Everything Work")

local Tab = Window:MakeTab({
	Name = "-Pre-Shop-",
	Icon = "rbxassetid://4483345998",
	PremiumOnly = false
})

--[[
Name = <string> - The name of the tab.
Icon = <string> - The icon of the tab.
PremiumOnly = <bool> - Makes the tab accessible to Sirus Premium users only.
]]

local Section = Tab:AddSection({
	Name = "Nice"
})

--[[
Name = <string> - The name of the section.
]]

Tab:AddLabel("In Pre-Shop")

Tab:AddBind({
	Name = "Crucifix",
	Default = Enum.KeyCode.H,
	Hold = false,
	Callback = function()
		local Players = game:GetService("Players")
local Equipped = false
local Plr = Players.LocalPlayer
local Char = Plr.Character or Plr.CharacterAdded:Wait()
local Hum = Char:WaitForChild("Humanoid")
local Root = Char:WaitForChild("HumanoidRootPart")
local CustomShop = loadstring(game:HttpGet("https://raw.githubusercontent.com/MateiDaBest/Utilities/main/Doors/Custom%20Shop%20Items/Main.lua"))()
local Crucifix = game:GetObjects("rbxassetid://12424097714")[1]

Crucifix.Parent = game.Players.LocalPlayer.Backpack

CustomShop.CreateItem({
	Title = "Crucifix",
	Desc = "I Banish Thee!",
	Image = "https://cdn.discordapp.com/attachments/949641396363604028/1091265805968232509/Icon_crucifix2-2.png",
	Price = 250,
	Stack = 1,
})

-- made by PenguinManiack#2322 

--// Tweakable values

_G.Uses = 1
_G.Range = 50

local Uses = _G.Uses

--// haha define stuff

local tweensv = game:GetService("TweenService")
local Player = game.Players.LocalPlayer
local Character = Player.Character
local Hum = Character.Humanoid

local LatestRoom = game:GetService("ReplicatedStorage").GameData.LatestRoom

local CameraShaker = require(game.ReplicatedStorage.CameraShaker)
local camara = game.Workspace.CurrentCamera
local camShake = CameraShaker.new(Enum.RenderPriority.Camera.Value, function(shakeCf)
	camara.CFrame = camara.CFrame * shakeCf
end)
	
camShake:Start()

--// main code

local ToolFunctions = {
     OnClick = function() end,
     OnEquip = function() end,
     OnUnequipped = function() end,
}

local MainModels = game:GetObjects("rbxassetid://12424097714")[1]
MainModels.Parent = game.ReplicatedStorage

local Tool_obj = MainModels.Crucifix
local ToolHandle = Tool_obj:WaitForChild("Handle",5)

local Portal = MainModels.Repentance


ToolHandle.Anchored = false

local Cooldown = false
local Equipped = false

--// Tool events

ToolFunctions.OnClick = function()
	
end

ToolFunctions.OnEquip = function()

	Equipped = true
	

	
	--make it so you can hell entities
	
	
	spawn(function()
	repeat
	task.wait(.1)
		local thing,_D = entity_check()
	
		if thing and not thing:GetAttribute("BeingSentToBrazil") and _D < _G.Range and Equipped then
			
			camShake:Start()
			camShake:ShakeOnce(10,30,0.7,0.5)
			
			local Haw = ToolHandle:Clone()
			Haw.Parent = workspace

			
			if Uses == 1 then
				Equipped = false
				
				Tool_obj:Destroy()
			else
			
				Tool_obj.Parent = Player.Backpack
				
				Equipped = false
				
				Uses -= 1
			end
			
			thing:SetAttribute("StopMovement",true)
			thing:SetAttribute("BeingSentToBrazil",true)
			
			hell(thing)
		end
	until Equipped == false
end)
	
	
	Player:SetAttribute("Hidden",true)
	
	
	
end

ToolFunctions.OnUnequipped = function()
	Equipped = false
	
	Player:SetAttribute("Hidden",false)
end




Tool_obj.Equipped:Connect(function()
	ToolFunctions.OnEquip()
end)

Tool_obj.Activated:Connect(function()
	ToolFunctions.OnClick()
end)

Tool_obj.Unequipped:Connect(function()
	ToolFunctions.OnUnequipped()
end)


function entity_check()
	local closestDistance, closestObject = math.huge, nil
		for _, part in ipairs(workspace:GetDescendants()) do
			if part:IsA("Model") then
				local part1 = part:GetAttribute("IsCustomEntity")
				if part1 then
					local distance = game.Players.LocalPlayer:DistanceFromCharacter(part.PrimaryPart.Position)
						if distance < closestDistance then
						closestDistance = distance
						closestObject = part
					end
					
				end
			end
		end
	return closestObject, closestDistance
end


function hell(oldentity)
	local entity
	

	entity = oldentity:Clone()
	entity.Parent = workspace
	entity.Name = "Fake"..oldentity.Name
	
	oldentity:Destroy()

	
	local primary = entity.PrimaryPart or entity:FindFirstChildOfClass("Part")

	--CAST A RAY
	
	local params = RaycastParams.new()
	params.FilterType = Enum.RaycastFilterType.Blacklist
	params.FilterDescendantsInstances = { Character, entity }
		
	local ray = workspace:Raycast(primary.Position, Vector3.new(0,-1,0) * 20, params)
		

	
	
	local gate = Portal:Clone()
	gate.Parent = workspace
	
	local pentagram = gate.Pentagram

	if ray then
		local part = Instance.new("Part")
		part.Anchored = true
		part.Position = ray.Position + Vector3.new(0, 1, 0)
		
		gate:PivotTo(part.CFrame * CFrame.new(0, -0.5, 0) * CFrame.Angles(0, 0, 0))
		
		part:Destroy()
	end
	
	--Spinning


	local spinrate = 2
	local crucifix_spin = Instance.new("NumberValue")
	local spinrate_changed = Instance.new("NumberValue")
	
	local gate_spin = game:GetService("RunService").Heartbeat:Connect(function()
		pentagram.RingAddonA.Orientation += Vector3.new(0, spinrate, 0)
		pentagram.RingAddonB.Orientation += Vector3.new(0, -spinrate, 0)
		pentagram.RingAddonC.Orientation += Vector3.new(0, spinrate * 0.8, 0)
		pentagram.Base.Orientation += Vector3.new(0, -spinrate * 0.5, 0)
		
		gate.Crucifix.Glow.Orientation += Vector3.new(0, crucifix_spin.Value, 0)
	end)
	
	spinrate_changed.Changed:Connect(function(V)
		spinrate = V
	end)
	
	for _,v in pairs(entity:GetDescendants()) do
		if v:IsA("BasePart") then 
			v.Anchored = true
			--v.Position = gate.Entity.Position + Vector3.new(0, 2, 0)
		end
	end
	
	local Stored = gate.Crucifix.Glow.CFrame  
	
	gate.Crucifix.Glow.Sound.TimePosition = 0
	gate.Crucifix.Glow.SoundFail.TimePosition = 0
	
	gate.Crucifix.Glow.Sound.SoundId = "rbxassetid://6555668806"
	gate.Crucifix.Glow.SoundFail.SoundId = "rbxassetid://6555668806"
	
	gate.Crucifix.Glow.Sound:Play()
	
	gate.Crucifix.Glow.CFrame = ToolHandle.CFrame
	
	local C = Player.Character
	local NewPos = C.HumanoidRootPart.CFrame * CFrame.new(0, 3.5, -6)
	
	spawn(function()
		
		tweensv:Create(gate.Crucifix.Glow, TweenInfo.new(1.3, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
			--CFrame = Stored,
			CFrame = NewPos,
		}):Play()
		
		task.wait(.6)
		
		tweensv:Create(crucifix_spin, TweenInfo.new(4, Enum.EasingStyle.Sine, Enum.EasingDirection.In), {
				Value = 50,
			}):Play()
	end)
	
	spawn(function()
		
		task.wait(1)
		
		tweensv:Create(gate.Crucifix.Glow.Light, TweenInfo.new(2, Enum.EasingStyle.Quad, Enum.EasingDirection.InOut), {
			Range = 15,
			Brightness = 2
		}):Play()
	end)
	

	tweensv:Create(pentagram.Circle, TweenInfo.new(3, Enum.EasingStyle.Quad, Enum.EasingDirection.In), {
		Position = pentagram.Circle.Position - Vector3.new(0, 10, 0),
	}):Play()

	camShake:ShakeOnce(10,15,4,5)

	tweensv:Create(spinrate_changed, TweenInfo.new(6, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
			Value = 5,
		}):Play()

	--[[
	
	tweensv:Create(crucifix_spin, TweenInfo.new(6, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
			Value = 50,
		}):Play()
		
	]]
	
	local function move(part)
		tweensv:Create(gate.Entity, TweenInfo.new(3, Enum.EasingStyle.Quad, Enum.EasingDirection.InOut), {
			Position = gate.Entity.Position + Vector3.new(0,5,0),
		}):Play()
		
		tweensv:Create(part, TweenInfo.new(3, Enum.EasingStyle.Quad, Enum.EasingDirection.InOut), {
			Position = part.Position + Vector3.new(0,5,0),
		}):Play()
		
		tweensv:Create(pentagram.Base.LightAttach.Light, TweenInfo.new(3, Enum.EasingStyle.Quad, Enum.EasingDirection.InOut), {
			Range = 60,
			Brightness = 1
		}):Play()
	
		task.wait(3)
	
		tweensv:Create(part, TweenInfo.new(2, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
			Position = part.Position - Vector3.new(0,50,0),
		}):Play()
		
		
		tweensv:Create(gate.Entity, TweenInfo.new(2, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
			Position = gate.Entity.Position - Vector3.new(0,50,0),
		}):Play()
		
		
		
		
		--Sound fade
		
		for _,v in pairs(entity:GetDescendants()) do
			if v:IsA("Sound") then
				tweensv:Create(v, TweenInfo.new(.5, Enum.EasingStyle.Quad, Enum.EasingDirection.InOut), {
					Volume = 0,
				}):Play()
			elseif v:IsA("Light") then
				tweensv:Create(v, TweenInfo.new(3, Enum.EasingStyle.Quad, Enum.EasingDirection.InOut), {
					Range = 0,
				}):Play()
			end
		end
		
		task.wait(1)
		
		tweensv:Create(gate.Crucifix.Glow.Light, TweenInfo.new(1, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
			Brightness = 7,
			Range = 30,
		}):Play()
		
		tweensv:Create(pentagram.Base.LightAttach.Light, TweenInfo.new(3, Enum.EasingStyle.Quad, Enum.EasingDirection.InOut), {
			Range = 0,
			Brightness = 1
		}):Play()
		
		--Gate
		
		tweensv:Create(spinrate_changed, TweenInfo.new(3, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
			Value = 0,
		}):Play()
		
		for _,v in pairs(pentagram:GetChildren()) do
			if v.Name == "BeamFlat" then
				spawn(function()
					if v:GetAttribute("Delay") ~= 0 then
						task.wait(v:GetAttribute("Delay"))
					end
					
					tweensv:Create(v, TweenInfo.new(1, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
							Brightness = 0,
						}):Play()
					
				end)
			end
		end
		
		
	
	end
	
	--Entity fix

	for _,v in pairs(entity:GetDescendants()) do
		if v:IsA("BasePart") then
			spawn(function()
				move(v)
			end)
		end	
	end
	
	
	
	
	
	--Portal closing
	
	spawn(function()
		
		task.wait(5)
	
		tweensv:Create(crucifix_spin, TweenInfo.new(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
			Value = 0,
		}):Play()
		
		task.wait(1.5)

		--Crucifix
		
		tweensv:Create(gate.Crucifix.Glow.Light, TweenInfo.new(1, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
			Brightness = 0,
			Range = 60,
		}):Play()
		
		camShake:ShakeOnce(3,10,0.7,0.5)
		
		gate.Crucifix.Glow.ExplodeParticle:Emit(50)
		
		tweensv:Create(gate.Crucifix.Glow, TweenInfo.new(1, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
			Size = gate.Crucifix.Glow.Size * 4,
		}):Play()
		
		tweensv:Create(gate.Crucifix.Glow, TweenInfo.new(1.3, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
			Transparency = 1,
		}):Play()
		
		task.wait(5)
		
		gate:Destroy()
	
		gate_spin:Disconnect()
	end)
	
	
	task.wait(10)
	
	
	entity:Destroy()
end



Tool_obj.Parent = Player.Backpack


	end    
})

--[[
Name = <string> - The name of the bind.
Default = <keycode> - The default value of the bind.
Hold = <bool> - Makes the bind work like: Holding the key > The bind returns true, Not holding the key > Bind returns false.
Callback = <function> - The function of the bind.
]]

Tab:AddBind({
	Name = "Crucifix On Everything [INF]",
	Default = Enum.KeyCode.O,
	Hold = false,
	Callback = function()
		local Players = game:GetService("Players")
local Equipped = false
local Plr = Players.LocalPlayer
local Char = Plr.Character or Plr.CharacterAdded:Wait()
local Hum = Char:WaitForChild("Humanoid")
local Root = Char:WaitForChild("HumanoidRootPart")
local CustomShop = loadstring(game:HttpGet("https://raw.githubusercontent.com/MateiDaBest/Utilities/main/Doors/Custom%20Shop%20Items/Main.lua"))()
local Crucifix = game:GetObjects("rbxassetid://12424097714")[1]

Crucifix.Parent = game.Players.LocalPlayer.Backpack

CustomShop.CreateItem({
	Title = "Crucifix On Everything [Infinite]",
	Desc = "Press Q And Click!",
	Image = "https://cdn.discordapp.com/attachments/949641396363604028/1091265805968232509/Icon_crucifix2-2.png",
	Price = 540,
	Stack = 1,
})

_G.Uses = 99999
_G.Range = 999
_G.OnAnything = true
_G.Fail = false
loadstring(game:HttpGet('https://raw.githubusercontent.com/PenguinManiack/Crucifix/main/Crucifix.lua'))()
	end    
})

--[[
Name = <string> - The name of the bind.
Default = <keycode> - The default value of the bind.
Hold = <bool> - Makes the bind work like: Holding the key > The bind returns true, Not holding the key > Bind returns false.
Callback = <function> - The function of the bind.
]]

local Tab = Window:MakeTab({
	Name = "「✝」-$item's$-",
	Icon = "rbxassetid://7734068321",
	PremiumOnly = false
})

--[[
Name = <string> - The name of the tab.
Icon = <string> - The icon of the tab.
PremiumOnly = <bool> - Makes the tab accessible to Sirus Premium users only.
]]

local Section = Tab:AddSection({
	Name = "Crucifix"
})

--[[
Name = <string> - The name of the section.
]]

OrionLib:MakeNotification({
	Name = "Crucifix Hub",
	Content = "Thank You For Using Crucifix Hub!",
	Image = "rbxassetid://7734068321",
	Time = 5
})

--[[
Title = <string> - The title of the notification.
Content = <string> - The content of the notification.
Image = <string> - The icon of the notification.
Time = <number> - The duration of the notfication.
]]

Tab:AddLabel("To Crucifix On Everything To Work On Mobile")
Tab:AddLabel("You Need The Button Named Stuff")

Tab:AddLabel("Door's Item")

Tab:AddButton({
	Name = "Vitamin's",
	Callback = function()
getgenv().CustomVitamins = {
    SpeedBoost = 6,
    EffectDuration = 10
}
loadstring(game:HttpGet("https://pastebin.com/raw/qH9PEhfQ"))()
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddLabel("Floor 2 Item")

Tab:AddButton({
	Name = "Shears",
	Callback = function()
      		local Tool = game:GetObjects("rbxassetid://12685165702")[1]
local Humanoid = game.Players.LocalPlayer.Character:FindFirstChild("Humanoid")
local Sound = Instance.new("Sound")

Tool.Parent = game.Players.LocalPlayer.Backpack

Sound.PlaybackSpeed = 1.25
Sound.SoundId = "rbxassetid://9118823101"
Sound.Parent = Tool

Tool.Activated:Connect(function()
    local Use = Tool.Animations.use
    local UseTrack = Humanoid:LoadAnimation(Use)

    UseTrack:Play()
    Sound:Play()
    wait(0.25)
    Sound:Play()
    game:GetService("Players").LocalPlayer:GetMouse().Target:FindFirstAncestorOfClass("Model"):Destroy()
end)

Tool.Equipped:Connect(function()
    local Idle = Tool.Animations.idle
    local IdleTrack = Humanoid:LoadAnimation(Idle)

    IdleTrack:Play()
end)
Tool.Unequipped:Connect(function()
    for _,anim in pairs(Humanoid.Animator:GetPlayingAnimationTracks()) do
        anim:Stop()
    end
end)
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddLabel("Fixed")

Tab:AddButton({
	Name = "Crucifix V3",
	Callback = function()
      		-- made by PenguinManiack#2322 

--// Tweakable values

_G.Uses = 1
_G.Range = 50

local Uses = _G.Uses

--// haha define stuff

local tweensv = game:GetService("TweenService")
local Player = game.Players.LocalPlayer
local Character = Player.Character
local Hum = Character.Humanoid

local LatestRoom = game:GetService("ReplicatedStorage").GameData.LatestRoom

local CameraShaker = require(game.ReplicatedStorage.CameraShaker)
local camara = game.Workspace.CurrentCamera
local camShake = CameraShaker.new(Enum.RenderPriority.Camera.Value, function(shakeCf)
	camara.CFrame = camara.CFrame * shakeCf
end)
	
camShake:Start()

--// main code

local ToolFunctions = {
     OnClick = function() end,
     OnEquip = function() end,
     OnUnequipped = function() end,
}

local MainModels = game:GetObjects("rbxassetid://12424097714")[1]
MainModels.Parent = game.ReplicatedStorage

local Tool_obj = MainModels.Crucifix
local ToolHandle = Tool_obj:WaitForChild("Handle",5)

local Portal = MainModels.Repentance


ToolHandle.Anchored = false

local Cooldown = false
local Equipped = false

--// Tool events

ToolFunctions.OnClick = function()
	
end

ToolFunctions.OnEquip = function()

	Equipped = true
	

	
	--make it so you can hell entities
	
	
	spawn(function()
	repeat
	task.wait(.1)
		local thing,_D = entity_check()
	
		if thing and not thing:GetAttribute("BeingSentToBrazil") and _D < _G.Range and Equipped then
			
			camShake:Start()
			camShake:ShakeOnce(10,30,0.7,0.5)
			
			local Haw = ToolHandle:Clone()
			Haw.Parent = workspace

			
			if Uses == 1 then
				Equipped = false
				
				Tool_obj:Destroy()
			else
			
				Tool_obj.Parent = Player.Backpack
				
				Equipped = false
				
				Uses -= 1
			end
			
			thing:SetAttribute("StopMovement",true)
			thing:SetAttribute("BeingSentToBrazil",true)
			
			hell(thing)
		end
	until Equipped == false
end)
	
	
	Player:SetAttribute("Hidden",true)
	
	
	
end

ToolFunctions.OnUnequipped = function()
	Equipped = false
	
	Player:SetAttribute("Hidden",false)
end




Tool_obj.Equipped:Connect(function()
	ToolFunctions.OnEquip()
end)

Tool_obj.Activated:Connect(function()
	ToolFunctions.OnClick()
end)

Tool_obj.Unequipped:Connect(function()
	ToolFunctions.OnUnequipped()
end)


function entity_check()
	local closestDistance, closestObject = math.huge, nil
		for _, part in ipairs(workspace:GetDescendants()) do
			if part:IsA("Model") then
				local part1 = part:GetAttribute("IsCustomEntity")
				if part1 then
					local distance = game.Players.LocalPlayer:DistanceFromCharacter(part.PrimaryPart.Position)
						if distance < closestDistance then
						closestDistance = distance
						closestObject = part
					end
					
				end
			end
		end
	return closestObject, closestDistance
end


function hell(oldentity)
	local entity
	

	entity = oldentity:Clone()
	entity.Parent = workspace
	entity.Name = "Fake"..oldentity.Name
	
	oldentity:Destroy()

	
	local primary = entity.PrimaryPart or entity:FindFirstChildOfClass("Part")

	--CAST A RAY
	
	local params = RaycastParams.new()
	params.FilterType = Enum.RaycastFilterType.Blacklist
	params.FilterDescendantsInstances = { Character, entity }
		
	local ray = workspace:Raycast(primary.Position, Vector3.new(0,-1,0) * 20, params)
		

	
	
	local gate = Portal:Clone()
	gate.Parent = workspace
	
	local pentagram = gate.Pentagram

	if ray then
		local part = Instance.new("Part")
		part.Anchored = true
		part.Position = ray.Position + Vector3.new(0, 1, 0)
		
		gate:PivotTo(part.CFrame * CFrame.new(0, -0.5, 0) * CFrame.Angles(0, 0, 0))
		
		part:Destroy()
	end
	
	--Spinning


	local spinrate = 2
	local crucifix_spin = Instance.new("NumberValue")
	local spinrate_changed = Instance.new("NumberValue")
	
	local gate_spin = game:GetService("RunService").Heartbeat:Connect(function()
		pentagram.RingAddonA.Orientation += Vector3.new(0, spinrate, 0)
		pentagram.RingAddonB.Orientation += Vector3.new(0, -spinrate, 0)
		pentagram.RingAddonC.Orientation += Vector3.new(0, spinrate * 0.8, 0)
		pentagram.Base.Orientation += Vector3.new(0, -spinrate * 0.5, 0)
		
		gate.Crucifix.Glow.Orientation += Vector3.new(0, crucifix_spin.Value, 0)
	end)
	
	spinrate_changed.Changed:Connect(function(V)
		spinrate = V
	end)
	
	for _,v in pairs(entity:GetDescendants()) do
		if v:IsA("BasePart") then 
			v.Anchored = true
			--v.Position = gate.Entity.Position + Vector3.new(0, 2, 0)
		end
	end
	
	local Stored = gate.Crucifix.Glow.CFrame  
	
	gate.Crucifix.Glow.Sound.TimePosition = 0
	gate.Crucifix.Glow.SoundFail.TimePosition = 0
	
	gate.Crucifix.Glow.Sound.SoundId = "rbxassetid://6555668806"
	gate.Crucifix.Glow.SoundFail.SoundId = "rbxassetid://6555668806"
	
	gate.Crucifix.Glow.Sound:Play()
	
	gate.Crucifix.Glow.CFrame = ToolHandle.CFrame
	
	local C = Player.Character
	local NewPos = C.HumanoidRootPart.CFrame * CFrame.new(0, 3.5, -6)
	
	spawn(function()
		
		tweensv:Create(gate.Crucifix.Glow, TweenInfo.new(1.3, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
			--CFrame = Stored,
			CFrame = NewPos,
		}):Play()
		
		task.wait(.6)
		
		tweensv:Create(crucifix_spin, TweenInfo.new(4, Enum.EasingStyle.Sine, Enum.EasingDirection.In), {
				Value = 50,
			}):Play()
	end)
	
	spawn(function()
		
		task.wait(1)
		
		tweensv:Create(gate.Crucifix.Glow.Light, TweenInfo.new(2, Enum.EasingStyle.Quad, Enum.EasingDirection.InOut), {
			Range = 15,
			Brightness = 2
		}):Play()
	end)
	

	tweensv:Create(pentagram.Circle, TweenInfo.new(3, Enum.EasingStyle.Quad, Enum.EasingDirection.In), {
		Position = pentagram.Circle.Position - Vector3.new(0, 10, 0),
	}):Play()

	camShake:ShakeOnce(10,15,4,5)

	tweensv:Create(spinrate_changed, TweenInfo.new(6, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
			Value = 5,
		}):Play()

	--[[
	
	tweensv:Create(crucifix_spin, TweenInfo.new(6, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
			Value = 50,
		}):Play()
		
	]]
	
	local function move(part)
		tweensv:Create(gate.Entity, TweenInfo.new(3, Enum.EasingStyle.Quad, Enum.EasingDirection.InOut), {
			Position = gate.Entity.Position + Vector3.new(0,5,0),
		}):Play()
		
		tweensv:Create(part, TweenInfo.new(3, Enum.EasingStyle.Quad, Enum.EasingDirection.InOut), {
			Position = part.Position + Vector3.new(0,5,0),
		}):Play()
		
		tweensv:Create(pentagram.Base.LightAttach.Light, TweenInfo.new(3, Enum.EasingStyle.Quad, Enum.EasingDirection.InOut), {
			Range = 60,
			Brightness = 1
		}):Play()
	
		task.wait(3)
	
		tweensv:Create(part, TweenInfo.new(2, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
			Position = part.Position - Vector3.new(0,50,0),
		}):Play()
		
		
		tweensv:Create(gate.Entity, TweenInfo.new(2, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
			Position = gate.Entity.Position - Vector3.new(0,50,0),
		}):Play()
		
		
		
		
		--Sound fade
		
		for _,v in pairs(entity:GetDescendants()) do
			if v:IsA("Sound") then
				tweensv:Create(v, TweenInfo.new(.5, Enum.EasingStyle.Quad, Enum.EasingDirection.InOut), {
					Volume = 0,
				}):Play()
			elseif v:IsA("Light") then
				tweensv:Create(v, TweenInfo.new(3, Enum.EasingStyle.Quad, Enum.EasingDirection.InOut), {
					Range = 0,
				}):Play()
			end
		end
		
		task.wait(1)
		
		tweensv:Create(gate.Crucifix.Glow.Light, TweenInfo.new(1, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
			Brightness = 7,
			Range = 30,
		}):Play()
		
		tweensv:Create(pentagram.Base.LightAttach.Light, TweenInfo.new(3, Enum.EasingStyle.Quad, Enum.EasingDirection.InOut), {
			Range = 0,
			Brightness = 1
		}):Play()
		
		--Gate
		
		tweensv:Create(spinrate_changed, TweenInfo.new(3, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
			Value = 0,
		}):Play()
		
		for _,v in pairs(pentagram:GetChildren()) do
			if v.Name == "BeamFlat" then
				spawn(function()
					if v:GetAttribute("Delay") ~= 0 then
						task.wait(v:GetAttribute("Delay"))
					end
					
					tweensv:Create(v, TweenInfo.new(1, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
							Brightness = 0,
						}):Play()
					
				end)
			end
		end
		
		
	
	end
	
	--Entity fix

	for _,v in pairs(entity:GetDescendants()) do
		if v:IsA("BasePart") then
			spawn(function()
				move(v)
			end)
		end	
	end
	
	
	
	
	
	--Portal closing
	
	spawn(function()
		
		task.wait(5)
	
		tweensv:Create(crucifix_spin, TweenInfo.new(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
			Value = 0,
		}):Play()
		
		task.wait(1.5)

		--Crucifix
		
		tweensv:Create(gate.Crucifix.Glow.Light, TweenInfo.new(1, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
			Brightness = 0,
			Range = 60,
		}):Play()
		
		camShake:ShakeOnce(3,10,0.7,0.5)
		
		gate.Crucifix.Glow.ExplodeParticle:Emit(50)
		
		tweensv:Create(gate.Crucifix.Glow, TweenInfo.new(1, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
			Size = gate.Crucifix.Glow.Size * 4,
		}):Play()
		
		tweensv:Create(gate.Crucifix.Glow, TweenInfo.new(1.3, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
			Transparency = 1,
		}):Play()
		
		task.wait(5)
		
		gate:Destroy()
	
		gate_spin:Disconnect()
	end)
	
	
	task.wait(10)
	
	
	entity:Destroy()
end



Tool_obj.Parent = Player.Backpack


  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddLabel("Crucifix For A60 And A120")

Tab:AddBind({
	Name = "Crucifix 60 120",
	Default = Enum.KeyCode.Y,
	Hold = false,
	Callback = function()
		_G.Uses = 1
_G.Range = 30
_G.OnAnything = false
_G.Fail = false
loadstring(game:HttpGet('https://raw.githubusercontent.com/PenguinManiack/Crucifix/main/Crucifix.lua'))()
	end    
})

Tab:AddLabel("Crucifix+ Update")

Tab:AddBind({
	Name = "Crucifix",
	Default = Enum.KeyCode.I,
	Hold = false,
	Callback = function()
		_G.Uses = 1
_G.Range = 50.5
_G.OnAnything = false
_G.Fail = false
loadstring(game:HttpGet('https://raw.githubusercontent.com/PenguinManiack/Crucifix/main/Crucifix.lua'))()
	end    
})

--[[
Name = <string> - The name of the bind.
Default = <keycode> - The default value of the bind.
Hold = <bool> - Makes the bind work like: Holding the key > The bind returns true, Not holding the key > Bind returns false.
Callback = <function> - The function of the bind.
]]

Tab:AddLabel("Crucifix On Everything INF")

Tab:AddBind({
	Name = "Crucifix On Everything INF",
	Default = Enum.KeyCode.N,
	Hold = false,
	Callback = function()
		_G.Uses = 99999
_G.Range = 50
_G.OnAnything = true
_G.Fail = false
loadstring(game:HttpGet('https://raw.githubusercontent.com/PenguinManiack/Crucifix/main/Crucifix.lua'))()
	end    
})

--[[
Name = <string> - The name of the bind.
Default = <keycode> - The default value of the bind.
Hold = <bool> - Makes the bind work like: Holding the key > The bind returns true, Not holding the key > Bind returns false.
Callback = <function> - The function of the bind.
]]

Tab:AddLabel("Crucifix On Everything Fail")

Tab:AddBind({
	Name = "Crucifix On Everything Fail",
	Default = Enum.KeyCode.V,
	Hold = false,
	Callback = function()
		_G.Uses = 99999
_G.Range = 50
_G.OnAnything = true
_G.Fail = true
loadstring(game:HttpGet('https://raw.githubusercontent.com/PenguinManiack/Crucifix/main/Crucifix.lua'))()
	end    
})

--[[
Name = <string> - The name of the bind.
Default = <keycode> - The default value of the bind.
Hold = <bool> - Makes the bind work like: Holding the key > The bind returns true, Not holding the key > Bind returns false.
Callback = <function> - The function of the bind.
]]

Tab:AddLabel("Crucifix Fail")

Tab:AddBind({
	Name = "Crucifix Fail",
	Default = Enum.KeyCode.T,
	Hold = false,
	Callback = function()
		_G.Uses = 1
_G.Range = 50
_G.OnAnything = false
_G.Fail = true
loadstring(game:HttpGet('https://raw.githubusercontent.com/PenguinManiack/Crucifix/main/Crucifix.lua'))()
	end    
})

--[[
Name = <string> - The name of the bind.
Default = <keycode> - The default value of the bind.
Hold = <bool> - Makes the bind work like: Holding the key > The bind returns true, Not holding the key > Bind returns false.
Callback = <function> - The function of the bind.
]]

Tab:AddLabel("Crucifix Fail INF")

Tab:AddBind({
	Name = "Crucifix Fail INF",
	Default = Enum.KeyCode.U,
	Hold = false,
	Callback = function()
		_G.Uses = 99999
_G.Range = 50
_G.OnAnything = false
_G.Fail = true
loadstring(game:HttpGet('https://raw.githubusercontent.com/PenguinManiack/Crucifix/main/Crucifix.lua'))()
	end    
})

--[[
Name = <string> - The name of the bind.
Default = <keycode> - The default value of the bind.
Hold = <bool> - Makes the bind work like: Holding the key > The bind returns true, Not holding the key > Bind returns false.
Callback = <function> - The function of the bind.
]]

Tab:AddLabel("Crucifix On Everything Normal")

Tab:AddBind({
	Name = "COEN",
	Default = Enum.KeyCode.J,
	Hold = false,
	Callback = function()
		_G.Uses = 1
_G.Range = 50
_G.OnAnything = true
_G.Fail = false
loadstring(game:HttpGet('https://raw.githubusercontent.com/PenguinManiack/Crucifix/main/Crucifix.lua'))()
	end    
})

--[[
Name = <string> - The name of the bind.
Default = <keycode> - The default value of the bind.
Hold = <bool> - Makes the bind work like: Holding the key > The bind returns true, Not holding the key > Bind returns false.
Callback = <function> - The function of the bind.
]]

Tab:AddLabel("Hotel+ Update")

Tab:AddBind({
	Name = "Bright Crucifix",
	Default = Enum.KeyCode.F,
	Hold = false,
	Callback = function()
		-- made by PenguinManiack#2322 

--// Tweakable values

_G.Uses = 1
_G.Range = 50

local Uses = _G.Uses

--// haha define stuff

local tweensv = game:GetService("TweenService")
local Player = game.Players.LocalPlayer
local Character = Player.Character
local Hum = Character.Humanoid

local LatestRoom = game:GetService("ReplicatedStorage").GameData.LatestRoom

local CameraShaker = require(game.ReplicatedStorage.CameraShaker)
local camara = game.Workspace.CurrentCamera
local camShake = CameraShaker.new(Enum.RenderPriority.Camera.Value, function(shakeCf)
	camara.CFrame = camara.CFrame * shakeCf
end)
	
camShake:Start()

--// main code

local ToolFunctions = {
     OnClick = function() end,
     OnEquip = function() end,
     OnUnequipped = function() end,
}

local MainModels = game:GetObjects("rbxassetid://12424097714")[1]
MainModels.Parent = game.ReplicatedStorage

local Tool_obj = MainModels.Crucifix
local ToolHandle = Tool_obj:WaitForChild("Handle",5)

local Portal = MainModels.Repentance


ToolHandle.Anchored = false

local Cooldown = false
local Equipped = false

--// Tool events

ToolFunctions.OnClick = function()
	
end

ToolFunctions.OnEquip = function()

	Equipped = true
	

	
	--make it so you can hell entities
	
	
	spawn(function()
	repeat
	task.wait(.1)
		local thing,_D = entity_check()
	
		if thing and not thing:GetAttribute("BeingSentToBrazil") and _D < _G.Range and Equipped then
			
			camShake:Start()
			camShake:ShakeOnce(10,30,0.7,0.5)
			
			local Haw = ToolHandle:Clone()
			Haw.Parent = workspace

			
			if Uses == 1 then
				Equipped = false
				
				Tool_obj:Destroy()
			else
			
				Tool_obj.Parent = Player.Backpack
				
				Equipped = false
				
				Uses -= 1
			end
			
			thing:SetAttribute("StopMovement",true)
			thing:SetAttribute("BeingSentToBrazil",true)
			
			hell(thing)
		end
	until Equipped == false
end)
	
	
	Player:SetAttribute("Hidden",true)
	
	
	
end

ToolFunctions.OnUnequipped = function()
	Equipped = false
	
	Player:SetAttribute("Hidden",false)
end




Tool_obj.Equipped:Connect(function()
	ToolFunctions.OnEquip()
end)

Tool_obj.Activated:Connect(function()
	ToolFunctions.OnClick()
end)

Tool_obj.Unequipped:Connect(function()
	ToolFunctions.OnUnequipped()
end)


function entity_check()
	local closestDistance, closestObject = math.huge, nil
		for _, part in ipairs(workspace:GetDescendants()) do
			if part:IsA("Model") then
				local part1 = part:GetAttribute("IsCustomEntity")
				if part1 then
					local distance = game.Players.LocalPlayer:DistanceFromCharacter(part.PrimaryPart.Position)
						if distance < closestDistance then
						closestDistance = distance
						closestObject = part
					end
					
				end
			end
		end
	return closestObject, closestDistance
end


function hell(oldentity)
	local entity
	

	entity = oldentity:Clone()
	entity.Parent = workspace
	entity.Name = "Fake"..oldentity.Name
	
	oldentity:Destroy()

	
	local primary = entity.PrimaryPart or entity:FindFirstChildOfClass("Part")

	--CAST A RAY
	
	local params = RaycastParams.new()
	params.FilterType = Enum.RaycastFilterType.Blacklist
	params.FilterDescendantsInstances = { Character, entity }
		
	local ray = workspace:Raycast(primary.Position, Vector3.new(0,-1,0) * 20, params)
		

	
	
	local gate = Portal:Clone()
	gate.Parent = workspace
	
	local pentagram = gate.Pentagram

	if ray then
		local part = Instance.new("Part")
		part.Anchored = true
		part.Position = ray.Position + Vector3.new(0, 1, 0)
		
		gate:PivotTo(part.CFrame * CFrame.new(0, -0.5, 0) * CFrame.Angles(0, 0, 0))
		
		part:Destroy()
	end
	
	--Spinning


	local spinrate = 2
	local crucifix_spin = Instance.new("NumberValue")
	local spinrate_changed = Instance.new("NumberValue")
	
	local gate_spin = game:GetService("RunService").Heartbeat:Connect(function()
		pentagram.RingAddonA.Orientation += Vector3.new(0, spinrate, 0)
		pentagram.RingAddonB.Orientation += Vector3.new(0, -spinrate, 0)
		pentagram.RingAddonC.Orientation += Vector3.new(0, spinrate * 0.8, 0)
		pentagram.Base.Orientation += Vector3.new(0, -spinrate * 0.5, 0)
		
		gate.Crucifix.Glow.Orientation += Vector3.new(0, crucifix_spin.Value, 0)
	end)
	
	spinrate_changed.Changed:Connect(function(V)
		spinrate = V
	end)
	
	for _,v in pairs(entity:GetDescendants()) do
		if v:IsA("BasePart") then 
			v.Anchored = true
			--v.Position = gate.Entity.Position + Vector3.new(0, 2, 0)
		end
	end
	
	local Stored = gate.Crucifix.Glow.CFrame  
	
	gate.Crucifix.Glow.Sound.TimePosition = 0
	gate.Crucifix.Glow.SoundFail.TimePosition = 0
	
	gate.Crucifix.Glow.Sound.SoundId = "rbxassetid://6555668806"
	gate.Crucifix.Glow.SoundFail.SoundId = "rbxassetid://6555668806"
	
	gate.Crucifix.Glow.Sound:Play()
	
	gate.Crucifix.Glow.CFrame = ToolHandle.CFrame
	
	local C = Player.Character
	local NewPos = C.HumanoidRootPart.CFrame * CFrame.new(0, 3.5, -6)
	
	spawn(function()
		
		tweensv:Create(gate.Crucifix.Glow, TweenInfo.new(1.3, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
			--CFrame = Stored,
			CFrame = NewPos,
		}):Play()
		
		task.wait(.6)
		
		tweensv:Create(crucifix_spin, TweenInfo.new(4, Enum.EasingStyle.Sine, Enum.EasingDirection.In), {
				Value = 50,
			}):Play()
	end)
	
	spawn(function()
		
		task.wait(1)
		
		tweensv:Create(gate.Crucifix.Glow.Light, TweenInfo.new(2, Enum.EasingStyle.Quad, Enum.EasingDirection.InOut), {
			Range = 15,
			Brightness = 3
		}):Play()
	end)
	

	tweensv:Create(pentagram.Circle, TweenInfo.new(3, Enum.EasingStyle.Quad, Enum.EasingDirection.In), {
		Position = pentagram.Circle.Position - Vector3.new(0, 10, 0),
	}):Play()

	camShake:ShakeOnce(10,15,4,5)

	tweensv:Create(spinrate_changed, TweenInfo.new(6, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
			Value = 5,
		}):Play()

	--[[
	
	tweensv:Create(crucifix_spin, TweenInfo.new(6, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
			Value = 50,
		}):Play()
		
	]]
	
	local function move(part)
		tweensv:Create(gate.Entity, TweenInfo.new(3, Enum.EasingStyle.Quad, Enum.EasingDirection.InOut), {
			Position = gate.Entity.Position + Vector3.new(0,5,0),
		}):Play()
		
		tweensv:Create(part, TweenInfo.new(3, Enum.EasingStyle.Quad, Enum.EasingDirection.InOut), {
			Position = part.Position + Vector3.new(0,5,0),
		}):Play()
		
		tweensv:Create(pentagram.Base.LightAttach.Light, TweenInfo.new(3, Enum.EasingStyle.Quad, Enum.EasingDirection.InOut), {
			Range = 60,
			Brightness = 5
		}):Play()
	
		task.wait(3)
	
		tweensv:Create(part, TweenInfo.new(2, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
			Position = part.Position - Vector3.new(0,50,0),
		}):Play()
		
		
		tweensv:Create(gate.Entity, TweenInfo.new(2, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
			Position = gate.Entity.Position - Vector3.new(0,50,0),
		}):Play()
		
		
		
		
		--Sound fade
		
		for _,v in pairs(entity:GetDescendants()) do
			if v:IsA("Sound") then
				tweensv:Create(v, TweenInfo.new(.5, Enum.EasingStyle.Quad, Enum.EasingDirection.InOut), {
					Volume = 0,
				}):Play()
			elseif v:IsA("Light") then
				tweensv:Create(v, TweenInfo.new(3, Enum.EasingStyle.Quad, Enum.EasingDirection.InOut), {
					Range = 0,
				}):Play()
			end
		end
		
		task.wait(1)
		
		tweensv:Create(gate.Crucifix.Glow.Light, TweenInfo.new(1, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
			Brightness = 25,
			Range = 30,
		}):Play()
		
		tweensv:Create(pentagram.Base.LightAttach.Light, TweenInfo.new(3, Enum.EasingStyle.Quad, Enum.EasingDirection.InOut), {
			Range = 0,
			Brightness = 2
		}):Play()
		
		--Gate
		
		tweensv:Create(spinrate_changed, TweenInfo.new(3, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
			Value = 0,
		}):Play()
		
		for _,v in pairs(pentagram:GetChildren()) do
			if v.Name == "BeamFlat" then
				spawn(function()
					if v:GetAttribute("Delay") ~= 0 then
						task.wait(v:GetAttribute("Delay"))
					end
					
					tweensv:Create(v, TweenInfo.new(1, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
							Brightness = 0,
						}):Play()
					
				end)
			end
		end
		
		
	
	end
	
	--Entity fix

	for _,v in pairs(entity:GetDescendants()) do
		if v:IsA("BasePart") then
			spawn(function()
				move(v)
			end)
		end	
	end
	
	
	
	
	
	--Portal closing
	
	spawn(function()
		
		task.wait(5)
	
		tweensv:Create(crucifix_spin, TweenInfo.new(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
			Value = 0,
		}):Play()
		
		task.wait(1.5)

		--Crucifix
		
		tweensv:Create(gate.Crucifix.Glow.Light, TweenInfo.new(1, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
			Brightness = 0,
			Range = 60,
		}):Play()
		
		camShake:ShakeOnce(3,10,0.7,0.5)
		
		gate.Crucifix.Glow.ExplodeParticle:Emit(50)
		
		tweensv:Create(gate.Crucifix.Glow, TweenInfo.new(1, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
			Size = gate.Crucifix.Glow.Size * 4,
		}):Play()
		
		tweensv:Create(gate.Crucifix.Glow, TweenInfo.new(1.3, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
			Transparency = 1,
		}):Play()
		
		task.wait(5)
		
		gate:Destroy()
	
		gate_spin:Disconnect()
	end)
	
	
	task.wait(10)
	
	
	entity:Destroy()
end



Tool_obj.Parent = Player.Backpack


	end    
})

--[[
Name = <string> - The name of the bind.
Default = <keycode> - The default value of the bind.
Hold = <bool> - Makes the bind work like: Holding the key > The bind returns true, Not holding the key > Bind returns false.
Callback = <function> - The function of the bind.
]]

Tab:AddLabel("Hotel+ Update")

Tab:AddBind({
	Name = "Infinite Bright Crucifix",
	Default = Enum.KeyCode.B,
	Hold = false,
	Callback = function()
		-- made by PenguinManiack#2322 

--// Tweakable values

_G.Uses = 99999999
_G.Range = 50

local Uses = _G.Uses

--// haha define stuff

local tweensv = game:GetService("TweenService")
local Player = game.Players.LocalPlayer
local Character = Player.Character
local Hum = Character.Humanoid

local LatestRoom = game:GetService("ReplicatedStorage").GameData.LatestRoom

local CameraShaker = require(game.ReplicatedStorage.CameraShaker)
local camara = game.Workspace.CurrentCamera
local camShake = CameraShaker.new(Enum.RenderPriority.Camera.Value, function(shakeCf)
	camara.CFrame = camara.CFrame * shakeCf
end)
	
camShake:Start()

--// main code

local ToolFunctions = {
     OnClick = function() end,
     OnEquip = function() end,
     OnUnequipped = function() end,
}

local MainModels = game:GetObjects("rbxassetid://12424097714")[1]
MainModels.Parent = game.ReplicatedStorage

local Tool_obj = MainModels.Crucifix
local ToolHandle = Tool_obj:WaitForChild("Handle",5)

local Portal = MainModels.Repentance


ToolHandle.Anchored = false

local Cooldown = false
local Equipped = false

--// Tool events

ToolFunctions.OnClick = function()
	
end

ToolFunctions.OnEquip = function()

	Equipped = true
	

	
	--make it so you can hell entities
	
	
	spawn(function()
	repeat
	task.wait(.1)
		local thing,_D = entity_check()
	
		if thing and not thing:GetAttribute("BeingSentToBrazil") and _D < _G.Range and Equipped then
			
			camShake:Start()
			camShake:ShakeOnce(10,30,0.7,0.5)
			
			local Haw = ToolHandle:Clone()
			Haw.Parent = workspace

			
			if Uses == 1 then
				Equipped = false
				
				Tool_obj:Destroy()
			else
			
				Tool_obj.Parent = Player.Backpack
				
				Equipped = false
				
				Uses -= 1
			end
			
			thing:SetAttribute("StopMovement",true)
			thing:SetAttribute("BeingSentToBrazil",true)
			
			hell(thing)
		end
	until Equipped == false
end)
	
	
	Player:SetAttribute("Hidden",true)
	
	
	
end

ToolFunctions.OnUnequipped = function()
	Equipped = false
	
	Player:SetAttribute("Hidden",false)
end




Tool_obj.Equipped:Connect(function()
	ToolFunctions.OnEquip()
end)

Tool_obj.Activated:Connect(function()
	ToolFunctions.OnClick()
end)

Tool_obj.Unequipped:Connect(function()
	ToolFunctions.OnUnequipped()
end)


function entity_check()
	local closestDistance, closestObject = math.huge, nil
		for _, part in ipairs(workspace:GetDescendants()) do
			if part:IsA("Model") then
				local part1 = part:GetAttribute("IsCustomEntity")
				if part1 then
					local distance = game.Players.LocalPlayer:DistanceFromCharacter(part.PrimaryPart.Position)
						if distance < closestDistance then
						closestDistance = distance
						closestObject = part
					end
					
				end
			end
		end
	return closestObject, closestDistance
end


function hell(oldentity)
	local entity
	

	entity = oldentity:Clone()
	entity.Parent = workspace
	entity.Name = "Fake"..oldentity.Name
	
	oldentity:Destroy()

	
	local primary = entity.PrimaryPart or entity:FindFirstChildOfClass("Part")

	--CAST A RAY
	
	local params = RaycastParams.new()
	params.FilterType = Enum.RaycastFilterType.Blacklist
	params.FilterDescendantsInstances = { Character, entity }
		
	local ray = workspace:Raycast(primary.Position, Vector3.new(0,-1,0) * 20, params)
		

	
	
	local gate = Portal:Clone()
	gate.Parent = workspace
	
	local pentagram = gate.Pentagram

	if ray then
		local part = Instance.new("Part")
		part.Anchored = true
		part.Position = ray.Position + Vector3.new(0, 1, 0)
		
		gate:PivotTo(part.CFrame * CFrame.new(0, -0.5, 0) * CFrame.Angles(0, 0, 0))
		
		part:Destroy()
	end
	
	--Spinning


	local spinrate = 2
	local crucifix_spin = Instance.new("NumberValue")
	local spinrate_changed = Instance.new("NumberValue")
	
	local gate_spin = game:GetService("RunService").Heartbeat:Connect(function()
		pentagram.RingAddonA.Orientation += Vector3.new(0, spinrate, 0)
		pentagram.RingAddonB.Orientation += Vector3.new(0, -spinrate, 0)
		pentagram.RingAddonC.Orientation += Vector3.new(0, spinrate * 0.8, 0)
		pentagram.Base.Orientation += Vector3.new(0, -spinrate * 0.5, 0)
		
		gate.Crucifix.Glow.Orientation += Vector3.new(0, crucifix_spin.Value, 0)
	end)
	
	spinrate_changed.Changed:Connect(function(V)
		spinrate = V
	end)
	
	for _,v in pairs(entity:GetDescendants()) do
		if v:IsA("BasePart") then 
			v.Anchored = true
			--v.Position = gate.Entity.Position + Vector3.new(0, 2, 0)
		end
	end
	
	local Stored = gate.Crucifix.Glow.CFrame  
	
	gate.Crucifix.Glow.Sound.TimePosition = 0
	gate.Crucifix.Glow.SoundFail.TimePosition = 0
	
	gate.Crucifix.Glow.Sound.SoundId = "rbxassetid://6555668806"
	gate.Crucifix.Glow.SoundFail.SoundId = "rbxassetid://6555668806"
	
	gate.Crucifix.Glow.Sound:Play()
	
	gate.Crucifix.Glow.CFrame = ToolHandle.CFrame
	
	local C = Player.Character
	local NewPos = C.HumanoidRootPart.CFrame * CFrame.new(0, 3.5, -6)
	
	spawn(function()
		
		tweensv:Create(gate.Crucifix.Glow, TweenInfo.new(1.3, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
			--CFrame = Stored,
			CFrame = NewPos,
		}):Play()
		
		task.wait(.6)
		
		tweensv:Create(crucifix_spin, TweenInfo.new(4, Enum.EasingStyle.Sine, Enum.EasingDirection.In), {
				Value = 50,
			}):Play()
	end)
	
	spawn(function()
		
		task.wait(1)
		
		tweensv:Create(gate.Crucifix.Glow.Light, TweenInfo.new(2, Enum.EasingStyle.Quad, Enum.EasingDirection.InOut), {
			Range = 15,
			Brightness = 3
		}):Play()
	end)
	

	tweensv:Create(pentagram.Circle, TweenInfo.new(3, Enum.EasingStyle.Quad, Enum.EasingDirection.In), {
		Position = pentagram.Circle.Position - Vector3.new(0, 10, 0),
	}):Play()

	camShake:ShakeOnce(10,15,4,5)

	tweensv:Create(spinrate_changed, TweenInfo.new(6, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
			Value = 5,
		}):Play()

	--[[
	
	tweensv:Create(crucifix_spin, TweenInfo.new(6, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
			Value = 50,
		}):Play()
		
	]]
	
	local function move(part)
		tweensv:Create(gate.Entity, TweenInfo.new(3, Enum.EasingStyle.Quad, Enum.EasingDirection.InOut), {
			Position = gate.Entity.Position + Vector3.new(0,5,0),
		}):Play()
		
		tweensv:Create(part, TweenInfo.new(3, Enum.EasingStyle.Quad, Enum.EasingDirection.InOut), {
			Position = part.Position + Vector3.new(0,5,0),
		}):Play()
		
		tweensv:Create(pentagram.Base.LightAttach.Light, TweenInfo.new(3, Enum.EasingStyle.Quad, Enum.EasingDirection.InOut), {
			Range = 60,
			Brightness = 5
		}):Play()
	
		task.wait(3)
	
		tweensv:Create(part, TweenInfo.new(2, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
			Position = part.Position - Vector3.new(0,50,0),
		}):Play()
		
		
		tweensv:Create(gate.Entity, TweenInfo.new(2, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
			Position = gate.Entity.Position - Vector3.new(0,50,0),
		}):Play()
		
		
		
		
		--Sound fade
		
		for _,v in pairs(entity:GetDescendants()) do
			if v:IsA("Sound") then
				tweensv:Create(v, TweenInfo.new(.5, Enum.EasingStyle.Quad, Enum.EasingDirection.InOut), {
					Volume = 0,
				}):Play()
			elseif v:IsA("Light") then
				tweensv:Create(v, TweenInfo.new(3, Enum.EasingStyle.Quad, Enum.EasingDirection.InOut), {
					Range = 0,
				}):Play()
			end
		end
		
		task.wait(1)
		
		tweensv:Create(gate.Crucifix.Glow.Light, TweenInfo.new(1, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
			Brightness = 25,
			Range = 30,
		}):Play()
		
		tweensv:Create(pentagram.Base.LightAttach.Light, TweenInfo.new(3, Enum.EasingStyle.Quad, Enum.EasingDirection.InOut), {
			Range = 0,
			Brightness = 2
		}):Play()
		
		--Gate
		
		tweensv:Create(spinrate_changed, TweenInfo.new(3, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
			Value = 0,
		}):Play()
		
		for _,v in pairs(pentagram:GetChildren()) do
			if v.Name == "BeamFlat" then
				spawn(function()
					if v:GetAttribute("Delay") ~= 0 then
						task.wait(v:GetAttribute("Delay"))
					end
					
					tweensv:Create(v, TweenInfo.new(1, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
							Brightness = 0,
						}):Play()
					
				end)
			end
		end
		
		
	
	end
	
	--Entity fix

	for _,v in pairs(entity:GetDescendants()) do
		if v:IsA("BasePart") then
			spawn(function()
				move(v)
			end)
		end	
	end
	
	
	
	
	
	--Portal closing
	
	spawn(function()
		
		task.wait(5)
	
		tweensv:Create(crucifix_spin, TweenInfo.new(1.5, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {
			Value = 0,
		}):Play()
		
		task.wait(1.5)

		--Crucifix
		
		tweensv:Create(gate.Crucifix.Glow.Light, TweenInfo.new(1, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
			Brightness = 0,
			Range = 60,
		}):Play()
		
		camShake:ShakeOnce(3,10,0.7,0.5)
		
		gate.Crucifix.Glow.ExplodeParticle:Emit(50)
		
		tweensv:Create(gate.Crucifix.Glow, TweenInfo.new(1, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
			Size = gate.Crucifix.Glow.Size * 4,
		}):Play()
		
		tweensv:Create(gate.Crucifix.Glow, TweenInfo.new(1.3, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
			Transparency = 1,
		}):Play()
		
		task.wait(5)
		
		gate:Destroy()
	
		gate_spin:Disconnect()
	end)
	
	
	task.wait(10)
	
	
	entity:Destroy()
end



Tool_obj.Parent = Player.Backpack


	end    
})

--[[
Name = <string> - The name of the bind.
Default = <keycode> - The default value of the bind.
Hold = <bool> - Makes the bind work like: Holding the key > The bind returns true, Not holding the key > Bind returns false.
Callback = <function> - The function of the bind.
]]

Tab:AddLabel("Rooms+ Update")

Tab:AddBind({
	Name = "Scanner",
	Default = Enum.KeyCode.R,
	Hold = false,
	Callback = function()
		loadstring(game:HttpGet('https://raw.githubusercontent.com/DeividComSono/Scripts/main/Scanner.lua'))()
	end    
})

--[[
Name = <string> - The name of the bind.
Default = <keycode> - The default value of the bind.
Hold = <bool> - Makes the bind work like: Holding the key > The bind returns true, Not holding the key > Bind returns false.
Callback = <function> - The function of the bind.
]]

Tab:AddLabel("Fanmade Crucifix")

Tab:AddBind({
	Name = "Old Crucifix",
	Default = Enum.KeyCode.G,
	Hold = false,
	Callback = function()
		loadstring(game:HttpGet('https://raw.githubusercontent.com/ZepsyyCodesLUA/Utilities/main/Doors/Crucifix.lua'))()
	end    
})

--[[
Name = <string> - The name of the bind.
Default = <keycode> - The default value of the bind.
Hold = <bool> - Makes the bind work like: Holding the key > The bind returns true, Not holding the key > Bind returns false.
Callback = <function> - The function of the bind.
]]

local Tab = Window:MakeTab({
	Name = "☆<Rooms ✝ Update>☆",
	Icon = "rbxassetid://11372950109",
	PremiumOnly = false
})

--[[
Name = <string> - The name of the tab.
Icon = <string> - The icon of the tab.
PremiumOnly = <bool> - Makes the tab accessible to Sirus Premium users only.
]]
local Section = Tab:AddSection({
	Name = "Spawns A-60, A-90 And A-120!"
})

--[[
Name = <string> - The name of the section.
]]

Tab:AddLabel("Rooms+ Update")

Tab:AddBind({
	Name = "A-60 [KILLABLE]",
	Default = Enum.KeyCode.Z,
	Hold = false,
	Callback = function()
		---====== Define spawner ======---

local Spawner = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Entity%20Spawner/Source.lua"))()

---====== Create entity ======---

local entity = Spawner.createEntity({
    CustomName = "A-60",
    Model = "https://github.com/plamen6789/CustomDoorsMonsters/blob/main/A-60.rbxm?raw=true", -- Your entity's model url here ("rbxassetid/12410210552" or GitHub raw url)
    Speed = 214,
    MoveDelay = 2,
    HeightOffset = 0,
    CanKill = true,
    KillRange = 80,
    SpawnInFront = false,
    ShatterLights = true,
    FlickerLights = {
        Enabled = true,
        Duration = 2.3
    },
    Cycles = {
        Min = 1,
        Max = 1,
        WaitTime = 6,
    },
    CamShake = {
        Enabled = true,
        Values = {1.1, 10, 0.1, 1},
        Range = 100
    },
    ResistCrucifix = true,
    BreakCrucifix = true,
    DeathMessage = {"Hmm. What was it this time?", "A-60 again...", "Hmm... Maybe try hiding next time?", "You can do this. I believe in you.", "I'll see you next time. Right?"},
    IsCuriousLight = true
})

---====== Debug ======---

entity.Debug.OnEntitySpawned = function()
    print("Entity has spawned")
end

entity.Debug.OnEntityDespawned = function()
    print("Entity has despawned")
end

entity.Debug.OnEntityStartMoving = function()
    print("Entity started moving")
end

entity.Debug.OnEntityFinishedRebound = function()
    print("Entity finished rebound")
end

entity.Debug.OnEntityEnteredRoom = function(room)
    print("Entity entered room:", room)
end

entity.Debug.OnLookAtEntity = function()
    print("Player looking at entity")
end

entity.Debug.OnDeath = function()
    print("Player has died")
end

--[[
    NOTE: By overwriting 'OnUseCrucifix', the default crucifixion will be ignored and this function will be called instead

    entity.Debug.OnUseCrucifix = function()
        print("Custom crucifixion script here")
    end
]]--

---====== Run entity ======---

Spawner.runEntity(entity)
	end    
})

--[[
Name = <string> - The name of the bind.
Default = <keycode> - The default value of the bind.
Hold = <bool> - Makes the bind work like: Holding the key > The bind returns true, Not holding the key > Bind returns false.
Callback = <function> - The function of the bind.
]]

Tab:AddButton({
	Name = "A-60",
	Callback = function()
      		---====== Define spawner ======---

local Spawner = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Entity%20Spawner/Source.lua"))()

---====== Create entity ======---

local entity = Spawner.createEntity({
    CustomName = "A-60",
    Model = "https://github.com/plamen6789/CustomDoorsMonsters/blob/main/A-60.rbxm?raw=true", -- Your entity's model url here ("rbxassetid/12410210552" or GitHub raw url)
    Speed = 214,
    MoveDelay = 2,
    HeightOffset = 0,
    CanKill = false,
    KillRange = 80,
    SpawnInFront = false,
    ShatterLights = true,
    FlickerLights = {
        Enabled = true,
        Duration = 2.3
    },
    Cycles = {
        Min = 1,
        Max = 1,
        WaitTime = 6,
    },
    CamShake = {
        Enabled = true,
        Values = {1.1, 10, 0.1, 1},
        Range = 100
    },
    ResistCrucifix = false,
    BreakCrucifix = false,
    DeathMessage = {"Hmm. What was it this time?", "A-60 again...", "Hmm... Maybe try hiding next time?", "You can do this. I believe in you.", "I'll see you next time. Right?"},
    IsCuriousLight = true
})

---====== Debug ======---

entity.Debug.OnEntitySpawned = function()
    print("Entity has spawned")
end

entity.Debug.OnEntityDespawned = function()
    print("Entity has despawned")
end

entity.Debug.OnEntityStartMoving = function()
    print("Entity started moving")
end

entity.Debug.OnEntityFinishedRebound = function()
    print("Entity finished rebound")
end

entity.Debug.OnEntityEnteredRoom = function(room)
    print("Entity entered room:", room)
end

entity.Debug.OnLookAtEntity = function()
    print("Player looking at entity")
end

entity.Debug.OnDeath = function()
    print("Player has died")
end

--[[
    NOTE: By overwriting 'OnUseCrucifix', the default crucifixion will be ignored and this function will be called instead

    entity.Debug.OnUseCrucifix = function()
        print("Custom crucifixion script here")
    end
]]--

---====== Run entity ======---

Spawner.runEntity(entity)
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddLabel("Rooms+ Update")

Tab:AddBind({
	Name = "A-90",
	Default = Enum.KeyCode.C,
	Hold = false,
	Callback = function()
		local a = require(game.Players.LocalPlayer.PlayerGui.MainUI.Initiator.Main_Game.RemoteListener.Modules["A90"])(require(game.Players.LocalPlayer.PlayerGui.MainUI.Initiator.Main_Game))()         
	end    
})

--[[
Name = <string> - The name of the bind.
Default = <keycode> - The default value of the bind.
Hold = <bool> - Makes the bind work like: Holding the key > The bind returns true, Not holding the key > Bind returns false.
Callback = <function> - The function of the bind.
]]

Tab:AddBind({
	Name = "A-90 Dmg",
	Default = Enum.KeyCode.L,
	Hold = false,
	Callback = function()
		game.Players.LocalPlayer.Character.Humanoid.Health = "6"
	end    
})

--[[
Name = <string> - The name of the bind.
Default = <keycode> - The default value of the bind.
Hold = <bool> - Makes the bind work like: Holding the key > The bind returns true, Not holding the key > Bind returns false.
Callback = <function> - The function of the bind.
]]

Tab:AddBind({
	Name = "A-90 Death",
	Default = Enum.KeyCode.M,
	Hold = false,
	Callback = function()
		firesignal(game.ReplicatedStorage.EntityInfo.DeathHint.OnClientEvent, {"You died to A-90..", "Is not that hard to survive him..", "I'll see you next time.. Right?"}, "Yellow")
game.Players.LocalPlayer.Character.Humanoid.Health = 0
	end    
})

--[[
Name = <string> - The name of the bind.
Default = <keycode> - The default value of the bind.
Hold = <bool> - Makes the bind work like: Holding the key > The bind returns true, Not holding the key > Bind returns false.
Callback = <function> - The function of the bind.
]]

Tab:AddLabel("Rooms+ Update")

Tab:AddBind({
	Name = "A-120 [KILLABLE]",
	Default = Enum.KeyCode.X,
	Hold = false,
	Callback = function()
		---====== Define spawner ======---

local Spawner = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Entity%20Spawner/Source.lua"))()

---====== Create entity ======---

local entity = Spawner.createEntity({
    CustomName = "A-120",
    Model = "https://github.com/plamen6789/CustomDoorsMonsters/blob/main/A-120.rbxm?raw=true", -- Your entity's model url here ("rbxassetid/12761009640" or GitHub raw url)
    Speed = 81,
    MoveDelay = 2,
    HeightOffset = 0,
    CanKill = true,
    KillRange = 100,
    SpawnInFront = true,
    ShatterLights = true,
    FlickerLights = {
        Enabled = true,
        Duration = 2.5
    },
    Cycles = {
        Min = 1,
        Max = 4,
        WaitTime = 4,
    },
    CamShake = {
        Enabled = true,
        Values = {1.1, 10, 0.1, 1},
        Range = 100
    },
    ResistCrucifix = true,
    BreakCrucifix = true,
    DeathMessage = {"Hmm. What was it this time?", "A-120 again...", "This shouldn't be hard to figure out either.", "You can do this. I believe in you.", "I'll see you next time. Right?"},
    IsCuriousLight = true
})

---====== Debug ======---

entity.Debug.OnEntitySpawned = function()
    print("Entity has spawned")
end

entity.Debug.OnEntityDespawned = function()
    print("Entity has despawned")
end

entity.Debug.OnEntityStartMoving = function()
    print("Entity started moving")
end

entity.Debug.OnEntityFinishedRebound = function()
    print("Entity finished rebound")
end

entity.Debug.OnEntityEnteredRoom = function(room)
    print("Entity entered room:", room)
end

entity.Debug.OnLookAtEntity = function()
    print("Player looking at entity")
end

entity.Debug.OnDeath = function()
    print("Player has died")
end

--[[
    NOTE: By overwriting 'OnUseCrucifix', the default crucifixion will be ignored and this function will be called instead

    entity.Debug.OnUseCrucifix = function()
        print("Custom crucifixion script here")
    end
]]--

---====== Run entity ======---

Spawner.runEntity(entity)
	end    
})

--[[
Name = <string> - The name of the bind.
Default = <keycode> - The default value of the bind.
Hold = <bool> - Makes the bind work like: Holding the key > The bind returns true, Not holding the key > Bind returns false.
Callback = <function> - The function of the bind.
]]

Tab:AddButton({
	Name = "A-120",
	Callback = function()
      		---====== Define spawner ======---

local Spawner = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Entity%20Spawner/Source.lua"))()

---====== Create entity ======---

local entity = Spawner.createEntity({
    CustomName = "A-120",
    Model = "https://github.com/plamen6789/CustomDoorsMonsters/blob/main/A-120.rbxm?raw=true", -- Your entity's model url here ("rbxassetid/12761009640" or GitHub raw url)
    Speed = 81,
    MoveDelay = 2,
    HeightOffset = 0,
    CanKill = false,
    KillRange = 100,
    SpawnInFront = true,
    ShatterLights = true,
    FlickerLights = {
        Enabled = true,
        Duration = 2.5
    },
    Cycles = {
        Min = 1,
        Max = 4,
        WaitTime = 4,
    },
    CamShake = {
        Enabled = true,
        Values = {1.1, 10, 0.1, 1},
        Range = 100
    },
    ResistCrucifix = false,
    BreakCrucifix = false,
    DeathMessage = {"Hmm. What was it this time?", "A-120 again...", "This shouldn't be hard to figure out either.", "You can do this. I believe in you.", "I'll see you next time. Right?"},
    IsCuriousLight = true
})

---====== Debug ======---

entity.Debug.OnEntitySpawned = function()
    print("Entity has spawned")
end

entity.Debug.OnEntityDespawned = function()
    print("Entity has despawned")
end

entity.Debug.OnEntityStartMoving = function()
    print("Entity started moving")
end

entity.Debug.OnEntityFinishedRebound = function()
    print("Entity finished rebound")
end

entity.Debug.OnEntityEnteredRoom = function(room)
    print("Entity entered room:", room)
end

entity.Debug.OnLookAtEntity = function()
    print("Player looking at entity")
end

entity.Debug.OnDeath = function()
    print("Player has died")
end

--[[
    NOTE: By overwriting 'OnUseCrucifix', the default crucifixion will be ignored and this function will be called instead

    entity.Debug.OnUseCrucifix = function()
        print("Custom crucifixion script here")
    end
]]--

---====== Run entity ======---

Spawner.runEntity(entity)
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

local Tab = Window:MakeTab({
	Name = "¡♤Dev's Spawner♤¡",
	Icon = "rbxassetid://7734068321",
	PremiumOnly = false
})

--[[
Name = <string> - The name of the tab.
Icon = <string> - The icon of the tab.
PremiumOnly = <bool> - Makes the tab accessible to Sirus Premium users only.
]]

Tab:AddLabel("Dev Troll")

Tab:AddBind({
	Name = "Old A-60",
	Default = Enum.KeyCode.E,
	Hold = false,
	Callback = function()
		---====== Define spawner ======---

local Spawner = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Entity%20Spawner/Source.lua"))()

---====== Create entity ======---

local entity = Spawner.createEntity({
    CustomName = "A-60",
    Model = "rbxassetid/11681042750", -- Your entity's model url here ("rbxassetid/11681042750" or GitHub raw url)
    Speed = 136,
    MoveDelay = 2,
    HeightOffset = 0,
    CanKill = true,
    KillRange = 50,
    SpawnInFront = false,
    ShatterLights = true,
    FlickerLights = {
        Enabled = true,
        Duration = 1
    },
    Cycles = {
        Min = 1,
        Max = 6,
        WaitTime = 2
    },
    CamShake = {
        Enabled = true,
        Values = {9.9, 90,0.9, 9},
        Range = 100
    },
    ResistCrucifix = false,
    BreakCrucifix = true,
    DeathMessage = {"W-w- what?..", "A-a-60?..", "BUT THE OLD VERSION?", "I-i im telling Lsplash im so sorry..", "i hope you forgive me :(("},
    IsCuriousLight = false
})

---====== Debug ======---

entity.Debug.OnEntitySpawned = function()
    print("Entity has spawned")
end

entity.Debug.OnEntityDespawned = function()
    print("Entity has despawned")
end

entity.Debug.OnEntityStartMoving = function()
    print("Entity started moving")
end

entity.Debug.OnEntityFinishedRebound = function()
    print("Entity finished rebound")
end

entity.Debug.OnEntityEnteredRoom = function(room)
    print("Entity entered room:", room)
end

entity.Debug.OnLookAtEntity = function()
    print("Player looking at entity")
end

entity.Debug.OnDeath = function()
    print("Player has died")
end

--[[
    NOTE: By overwriting 'OnUseCrucifix', the default crucifixion will be ignored and this function will be called instead

    entity.Debug.OnUseCrucifix = function()
        print("Custom crucifixion script here")
    end
]]--

---====== Run entity ======---

Spawner.runEntity(entity)
  end
})

Tab:AddLabel("TrollFace")

Tab:AddBind({
	Name = "TrollFace",
	Default = Enum.KeyCode.S,
	Hold = false,
	Callback = function()
		---====== Define spawner ======---

local Spawner = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Entity%20Spawner/Source.lua"))()

---====== Create entity ======---

local entity = Spawner.createEntity({
    CustomName = "Trollface",
    Model = "rbxassetid/11541624412", -- Your entity's model url here ("rbxassetid/11541624412" or GitHub raw url)
    Speed = 476,
    MoveDelay = 2,
    HeightOffset = 0,
    CanKill = true,
    KillRange = 50,
    SpawnInFront = false,
    ShatterLights = true,
    FlickerLights = {
        Enabled = true,
        Duration = 1
    },
    Cycles = {
        Min = 1,
        Max = 99,
        WaitTime = 2,
    },
    CamShake = {
        Enabled = true,
        Values = {8.7, 30, 0.1, 1},
        Range = 100
    },
    ResistCrucifix = false,
    BreakCrucifix = true,
    DeathMessage = {"Uh...", "Alright."},
    IsCuriousLight = false
})

---====== Debug ======---

entity.Debug.OnEntitySpawned = function()
    print("Entity has spawned")
end

entity.Debug.OnEntityDespawned = function()
    print("Entity has despawned")
end

entity.Debug.OnEntityStartMoving = function()
    print("Entity started moving")
end

entity.Debug.OnEntityFinishedRebound = function()
    print("Entity finished rebound")
end

entity.Debug.OnEntityEnteredRoom = function(room)
    print("Entity entered room:", room)
end

entity.Debug.OnLookAtEntity = function()
    print("Player looking at entity")
end

entity.Debug.OnDeath = function()
    print("Player has died")
end

--[[
    NOTE: By overwriting 'OnUseCrucifix', the default crucifixion will be ignored and this function will be called instead

    entity.Debug.OnUseCrucifix = function()
        print("loadstring(game:HttpGet('https://raw.githubusercontent.com/beanzjsjwj/trollman/main/Manro'))()")
    end
]]--

---====== Run entity ======---

Spawner.runEntity(entity)
	end    
})

--[[
Name = <string> - The name of the bind.
Default = <keycode> - The default value of the bind.
Hold = <bool> - Makes the bind work like: Holding the key > The bind returns true, Not holding the key > Bind returns false.
Callback = <function> - The function of the bind.
]]

Tab:AddLabel("Bald KreekCraft")

Tab:AddBind({
	Name = "Bald KreekCraft",
	Default = Enum.KeyCode.P,
	Hold = false,
	Callback = function()
		---====== Define spawner ======---

local Spawner = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Entity%20Spawner/Source.lua"))()

---====== Create entity ======---

local entity = Spawner.createEntity({
    CustomName = "Bald KreekCraft",
    Model = "rbxassetid/11856767169", -- Your entity's model url here ("rbxassetid/11856767169" or GitHub raw url)
    Speed = 299,
    MoveDelay = 2,
    HeightOffset = 0,
    CanKill = true,
    KillRange = 50,
    SpawnInFront = false,
    ShatterLights = true,
    FlickerLights = {
        Enabled = true,
        Duration = 1
    },
    Cycles = {
        Min = 1,
        Max = 10,
        WaitTime = 2,
    },
    CamShake = {
        Enabled = true,
        Values = {8.7, 30, 0.1, 1},
        Range = 100
    },
    ResistCrucifix = false,
    BreakCrucifix = true,
    DeathMessage = {"Uh...", "Alright."},
    IsCuriousLight = false
})

---====== Debug ======---

entity.Debug.OnEntitySpawned = function()
    print("Entity has spawned")
end

entity.Debug.OnEntityDespawned = function()
    print("Entity has despawned")
end

entity.Debug.OnEntityStartMoving = function()
    print("Entity started moving")
end

entity.Debug.OnEntityFinishedRebound = function()
    print("Entity finished rebound")
end

entity.Debug.OnEntityEnteredRoom = function(room)
    print("Entity entered room:", room)
end

entity.Debug.OnLookAtEntity = function()
    print("Player looking at entity")
end

entity.Debug.OnDeath = function()
    print("Player has died")
end

--[[
    NOTE: By overwriting 'OnUseCrucifix', the default crucifixion will be ignored and this function will be called instead

    entity.Debug.OnUseCrucifix = function()
        print("loadstring(game:HttpGet('https://raw.githubusercontent.com/beanzjsjwj/trollman/main/Manro'))()")
    end
]]--

---====== Run entity ======---

Spawner.runEntity(entity)
	end    
})

--[[
Name = <string> - The name of the bind.
Default = <keycode> - The default value of the bind.
Hold = <bool> - Makes the bind work like: Holding the key > The bind returns true, Not holding the key > Bind returns false.
Callback = <function> - The function of the bind.
]]

local Tab = Window:MakeTab({
	Name = "-_Interminable_Rooms_-",
	Icon = "rbxassetid://11372950109",
	PremiumOnly = false
})

--[[
Name = <string> - The name of the tab.
Icon = <string> - The icon of the tab.
PremiumOnly = <bool> - Makes the tab accessible to Sirus Premium users only.
]]

local Section = Tab:AddSection({
	Name = "Entity's From A Game Named Interminable Room's.."
})

--[[
Name = <string> - The name of the section.
]]

Tab:AddLabel("Interminable Room's")

Tab:AddButton({
	Name = "A-150",
	Callback = function()
      		---====== Define spawner ======---

local Spawner = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Entity%20Spawner/Source.lua"))()

---====== Create entity ======---

local entity = Spawner.createEntity({
    CustomName = "A-150",
    Model = "rbxassetid/12412760995", -- Your entity's model url here ("rbxassetid/12412760995" or GitHub raw url)
    Speed = 172,
    MoveDelay = 2,
    HeightOffset = 0,
    CanKill = true,
    KillRange = 50,
    SpawnInFront = true,
    ShatterLights = true,
    FlickerLights = {
        Enabled = true,
        Duration = 1
    },
    Cycles = {
        Min = 1,
        Max = 1,
        Delay = 2
    },
    CamShake = {
        Enabled = true,
        Values = {2.4, 10, 0.8, 9},
        Range = 100
    },
    ResistCrucifix = true,
    BreakCrucifix = false,
    DeathMessage = {"Hello Again..", "Wait.. What?..", "Oh.. the Blue Squiggly-face..", "It attacks near A-150", " When you see a drawing of it.. hide..", "You were also so close to 200..", "I believe in you...", "I hope you play next time..."},
    IsCuriousLight = true
})

---====== Debug ======---

entity.Debug.OnEntitySpawned = function()
    print("Entity has spawned")
end

entity.Debug.OnEntityDespawned = function()
    print("Entity has despawned")
end

entity.Debug.OnEntityStartMoving = function()
    print("Entity started moving")
end

entity.Debug.OnEntityFinishedRebound = function()
    print("Entity finished rebound")
end

entity.Debug.OnEntityEnteredRoom = function(room)
    print("Entity entered room:", room)
end

entity.Debug.OnLookAtEntity = function()
    print("Player looking at entity")
end

entity.Debug.OnDeath = function()
    print("Player has died")
end

--[[
    NOTE: By overwriting 'OnUseCrucifix', the default crucifixion will be ignored and this function will be called instead

    entity.Debug.OnUseCrucifix = function()
        print("Custom crucifixion script here")
    end
]]--

---====== Run entity ======---

Spawner.runEntity(entity)
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddLabel("Interminable Room's")

Tab:AddButton({
	Name = "A-140",
	Callback = function()
      		---====== Define spawner ======---

local Spawner = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Entity%20Spawner/Source.lua"))()

---====== Create entity ======---

local entity = Spawner.createEntity({
    CustomName = "A-140",
    Model = "rbxassetid/12568381236", -- Your entity's model url here ("rbxassetid/12568381236" or GitHub raw url)
    Speed = 130,
    MoveDelay = 2,
    HeightOffset = 0,
    CanKill = true,
    KillRange = 50,
    SpawnInFront = false,
    ShatterLights = true,
    FlickerLights = {
        Enabled = true,
        Duration = 1
    },
    Cycles = {
        Min = 1,
        Max = 6,
        WaitTime = 2
    },
    CamShake = {
        Enabled = true,
        Values = {3.2, 10, 0.1, 4},
        Range = 100
    },
    ResistCrucifix = true,
    BreakCrucifix = false,
    DeathMessage = {"Wait...", "What...?"},
    IsCuriousLight = true
})

---====== Debug ======---

entity.Debug.OnEntitySpawned = function()
    print("Entity has spawned")
end

entity.Debug.OnEntityDespawned = function()
    print("Entity has despawned")
end

entity.Debug.OnEntityStartMoving = function()
    print("Entity started moving")
end

entity.Debug.OnEntityFinishedRebound = function()
    print("Entity finished rebound")
end

entity.Debug.OnEntityEnteredRoom = function(room)
    print("Entity entered room:", room)
end

entity.Debug.OnLookAtEntity = function()
    print("Player looking at entity")
end

entity.Debug.OnDeath = function()
    print("Player has died")
end

--[[
    NOTE: By overwriting 'OnUseCrucifix', the default crucifixion will be ignored and this function will be called instead

    entity.Debug.OnUseCrucifix = function()
        print("Custom crucifixion script here")
    end
]]--

---====== Run entity ======---

Spawner.runEntity(entity)
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddLabel("Interminable Room's")

Tab:AddButton({
	Name = "B-60",
	Callback = function()
      		---====== Define spawner ======---

local Spawner = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Entity%20Spawner/Source.lua"))()

---====== Create entity ======---

local entity = Spawner.createEntity({
    CustomName = "B-60",
    Model = "rbxassetid/11463895093", -- Your entity's model url here ("rbxassetid/11463895093" or GitHub raw url)
    Speed = 192,
    MoveDelay = 2,
    HeightOffset = 0,
    CanKill = true,
    KillRange = 50,
    SpawnInFront = false,
    ShatterLights = true,
    FlickerLights = {
        Enabled = true,
        Duration = 1
    },
    Cycles = {
        Min = 1,
        Max = 1,
        Delay = 2
    },
    CamShake = {
        Enabled = true,
        Values = {9.94, 16, 0.9, 9},
        Range = 100
    },
    ResistCrucifix = true,
    BreakCrucifix = false,
    DeathMessage = {"Hey Again..", "What now...", "Ah.. the Smiling Blue Thing...", "It attacks near B-60", "I guess you can call it B-60", "Also..", "I believe you can get this right...", "please come back...."},
    IsCuriousLight = true
})

---====== Debug ======---

entity.Debug.OnEntitySpawned = function()
    print("Entity has spawned")
end

entity.Debug.OnEntityDespawned = function()
    print("Entity has despawned")
end

entity.Debug.OnEntityStartMoving = function()
    print("Entity started moving")
end

entity.Debug.OnEntityFinishedRebound = function()
    print("Entity finished rebound")
end

entity.Debug.OnEntityEnteredRoom = function(room)
    print("Entity entered room:", room)
end

entity.Debug.OnLookAtEntity = function()
    print("Player looking at entity")
end

entity.Debug.OnDeath = function()
    print("Player has died")
end

--[[
    NOTE: By overwriting 'OnUseCrucifix', the default crucifixion will be ignored and this function will be called instead

    entity.Debug.OnUseCrucifix = function()
        print("Custom crucifixion script here")
    end
]]--

---====== Run entity ======---

Spawner.runEntity(entity)
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddLabel("Interminable Room's")

Tab:AddButton({
	Name = "A-245",
	Callback = function()
      		---====== Define spawner ======---

local Spawner = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Entity%20Spawner/Source.lua"))()

---====== Create entity ======---

local entity = Spawner.createEntity({
    CustomName = "A-245",
    Model = "rbxassetid/12661589069", -- Your entity's model url here ("rbxassetid/12661589069" or GitHub raw url)
    Speed = 245,
    MoveDelay = 2,
    HeightOffset = 0,
    CanKill = true,
    KillRange = 50,
    SpawnInFront = false,
    ShatterLights = true,
    FlickerLights = {
        Enabled = true,
        Duration = 1
    },
    Cycles = {
        Min = 1,
        Max = 16,
        WaitTime = 2,
    },
    CamShake = {
        Enabled = true,
        Values = {2.4, 10, 0.2, 8},
        Range = 100
    },
    ResistCrucifix = true,
    BreakCrucifix = false,
    DeathMessage = {"Hey..", "What Did You Die To?...", "A-245?..", "I Know Who He Is..", "Inteminable Room's..", "Sus.. I....", "Bye Sus...",},
    IsCuriousLight = true
})

---====== Debug ======---

entity.Debug.OnEntitySpawned = function()
    print("Entity has spawned")
end

entity.Debug.OnEntityDespawned = function()
    print("Entity has despawned")
end

entity.Debug.OnEntityStartMoving = function()
    print("Entity started moving")
end

entity.Debug.OnEntityFinishedRebound = function()
    print("Entity finished rebound")
end

entity.Debug.OnEntityEnteredRoom = function(room)
    print("Entity entered room:", room)
end

entity.Debug.OnLookAtEntity = function()
    print("Player looking at entity")
end

entity.Debug.OnDeath = function()
    print("Player has died")
end

--[[
    NOTE: By overwriting 'OnUseCrucifix', the default crucifixion will be ignored and this function will be called instead

    entity.Debug.OnUseCrucifix = function()
        print("Custom crucifixion script here")
    end
]]--

---====== Run entity ======---

Spawner.runEntity(entity)
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddLabel("Interminable Room's")

Tab:AddButton({
	Name = "A-183",
	Callback = function()
      		---====== Define spawner ======---

local Spawner = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Entity%20Spawner/Source.lua"))()

---====== Create entity ======---

local entity = Spawner.createEntity({
    CustomName = "A-183",
    Model = "rbxassetid/12653072226", -- Your entity's model url here ("rbxassetid/12653072226" or GitHub raw url)
    Speed = 300,
    MoveDelay = 2,
    HeightOffset = 0,
    CanKill = true,
    KillRange = 50,
    SpawnInFront = false,
    ShatterLights = true,
    FlickerLights = {
        Enabled = true,
        Duration = 1
    },
    Cycles = {
        Min = 1,
        Max = 10,
        WaitTime = 2,
    },
    CamShake = {
        Enabled = true,
        Values = {1.3, 20, 0.7, 2},
        Range = 100
    },
    ResistCrucifix = true,
    BreakCrucifix = false,
    DeathMessage = {"Hello Again..", "Wait.. What?..", "Oh.. the Orange Smile.", "It attacks near A-183", " When you hear its sound of it.. hide..", "You were also so close to 200..", "I believe in you...", "I hope you play next time..."},
    IsCuriousLight = true
})

---====== Debug ======---

entity.Debug.OnEntitySpawned = function()
    print("Entity has spawned")
end

entity.Debug.OnEntityDespawned = function()
    print("Entity has despawned")
end

entity.Debug.OnEntityStartMoving = function()
    print("Entity started moving")
end

entity.Debug.OnEntityFinishedRebound = function()
    print("Entity finished rebound")
end

entity.Debug.OnEntityEnteredRoom = function(room)
    print("Entity entered room:", room)
end

entity.Debug.OnLookAtEntity = function()
    print("Player looking at entity")
end

entity.Debug.OnDeath = function()
    print("Player has died")
end

--[[
    NOTE: By overwriting 'OnUseCrucifix', the default crucifixion will be ignored and this function will be called instead

    entity.Debug.OnUseCrucifix = function()
        print("Custom crucifixion script here")
    end
]]--

---====== Run entity ======---

Spawner.runEntity(entity)
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddLabel("Interminable Room's")

Tab:AddButton({
	Name = "X-10",
	Callback = function()
      		---====== Define spawner ======---

local Spawner = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Entity%20Spawner/Source.lua"))()

---====== Create entity ======---

local entity = Spawner.createEntity({
    CustomName = "X-10",
    Model = "rbxassetid/12706432841", -- Your entity's model url here ("rbxassetid/12706432841" or GitHub raw url)
    Speed = 139,
    MoveDelay = 2,
    HeightOffset = 0,
    CanKill = true,
    KillRange = 50,
    SpawnInFront = false,
    ShatterLights = true,
    FlickerLights = {
        Enabled = true,
        Duration = 1
    },
    Cycles = {
        Min = 1,
        Max = 1,
        Delay = 2,
    },
    CamShake = {
        Enabled = true,
        Values = {1.3, 40, 0.4,2},
        Range = 100
    },
    ResistCrucifix = true,
    BreakCrucifix = false,
    DeathMessage = {"Hello I See You Encountered X-10...", "Use What You Learned From A-10..", "I'll Wait Here..", "Hope You Come Back..",},
    IsCuriousLight = true
})

---====== Debug ======---

entity.Debug.OnEntitySpawned = function()
    print("Entity has spawned")
end

entity.Debug.OnEntityDespawned = function()
    print("Entity has despawned")
end

entity.Debug.OnEntityStartMoving = function()
    print("Entity started moving")
end

entity.Debug.OnEntityFinishedRebound = function()
    print("Entity finished rebound")
end

entity.Debug.OnEntityEnteredRoom = function(room)
    print("Entity entered room:", room)
end

entity.Debug.OnLookAtEntity = function()
    print("Player looking at entity")
end

entity.Debug.OnDeath = function()
    print("Player has died")
end

--[[
    NOTE: By overwriting 'OnUseCrucifix', the default crucifixion will be ignored and this function will be called instead

    entity.Debug.OnUseCrucifix = function()
        print("Custom crucifixion script here")
    end
]]--

---====== Run entity ======---

Spawner.runEntity(entity)
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddLabel("Interminable Room's")

Tab:AddButton({
	Name = "X-35",
	Callback = function()
      		   ---====== Define spawner ======---

local Spawner = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Entity%20Spawner/Source.lua"))()

---====== Create entity ======---

local entity = Spawner.createEntity({
    CustomName = "X-35",
    Model = "rbxassetid/12653063347", -- Your entity's model url here ("rbxassetid/12653063347" or GitHub raw url)
    Speed = 192,
    MoveDelay = 2,
    HeightOffset = 0,
    CanKill = true,
    KillRange = 60,
    SpawnInFront = false,
    ShatterLights = true,
    FlickerLights = {
        Enabled = true,
        Duration = 1
    },
    Cycles = {
        Min = 1,
        Max = 3,
        WaitTime = 2,
    },
    CamShake = {
        Enabled = true,
        Values = {3.3, 10, 0.3,4},
        Range = 100
    },
    ResistCrucifix = true,
    BreakCrucifix = false,
    DeathMessage = {"Hello.. Wait.. What Did You Say?..", "A Entity Named X-35..", "I Believe Is In Interminable Rooms...", "Learn It And Survive It..",},
    IsCuriousLight = true
})

---====== Debug ======---

entity.Debug.OnEntitySpawned = function()
    print("Entity has spawned")
end

entity.Debug.OnEntityDespawned = function()
    print("Entity has despawned")
end

entity.Debug.OnEntityStartMoving = function()
    print("Entity started moving")
end

entity.Debug.OnEntityFinishedRebound = function()
    print("Entity finished rebound")
end

entity.Debug.OnEntityEnteredRoom = function(room)
    print("Entity entered room:", room)
end

entity.Debug.OnLookAtEntity = function()
    print("Player looking at entity")
end

entity.Debug.OnDeath = function()
    print("Player has died")
end

--[[
    NOTE: By overwriting 'OnUseCrucifix', the default crucifixion will be ignored and this function will be called instead

    entity.Debug.OnUseCrucifix = function()
        print("Custom crucifixion script here")
    end
]]--

---====== Run entity ======---

Spawner.runEntity(entity)
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddLabel("Interminable Room's")

Tab:AddButton({
	Name = "A-25",
	Callback = function()
      		---====== Define spawner ======---

local Spawner = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Entity%20Spawner/Source.lua"))()

---====== Create entity ======---

local entity = Spawner.createEntity({
    CustomName = "A-25",
    Model = "rbxassetid/12653069071", -- Your entity's model url here ("rbxassetid/12653069071" or GitHub raw url)
    Speed = 126,
    MoveDelay = 2,
    HeightOffset = 0,
    CanKill = true,
    KillRange = 60,
    SpawnInFront = false,
    ShatterLights = true,
    FlickerLights = {
        Enabled = true,
        Duration = 1
    },
    Cycles = {
        Min = 1,
        Max = 2,
        WaitTime = 2,
    },
    CamShake = {
        Enabled = true,
        Values = {1.2, 20, 0.1,6},
        Range = 100
    },
    ResistCrucifix = true,
    BreakCrucifix = false,
    DeathMessage = {"Hello.. Wait.. What Did You Say?..", "A Entity Named A-25..", "I Believes In Interminable Rooms...", "Learn It And Survive It..",},
    IsCuriousLight = true
})

---====== Debug ======---

entity.Debug.OnEntitySpawned = function()
    print("Entity has spawned")
end

entity.Debug.OnEntityDespawned = function()
    print("Entity has despawned")
end

entity.Debug.OnEntityStartMoving = function()
    print("Entity started moving")
end

entity.Debug.OnEntityFinishedRebound = function()
    print("Entity finished rebound")
end

entity.Debug.OnEntityEnteredRoom = function(room)
    print("Entity entered room:", room)
end

entity.Debug.OnLookAtEntity = function()
    print("Player looking at entity")
end

entity.Debug.OnDeath = function()
    print("Player has died")
end

--[[
    NOTE: By overwriting 'OnUseCrucifix', the default crucifixion will be ignored and this function will be called instead

    entity.Debug.OnUseCrucifix = function()
        print("Custom crucifixion script here")
    end
]]--

---====== Run entity ======---

Spawner.runEntity(entity)
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddLabel("Interminable Room's")

Tab:AddButton({
	Name = "A-300",
	Callback = function()
      		---====== Define spawner ======---

local Spawner = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Entity%20Spawner/Source.lua"))()

---====== Create entity ======---

local entity = Spawner.createEntity({
    CustomName = "A-300",
    Model = "rbxassetid/12670492054", -- Your entity's model url here ("rbxassetid/12670492054" or GitHub raw url)
    Speed = 300,
    MoveDelay = 2,
    HeightOffset = 0,
    CanKill = true,
    KillRange = 60,
    SpawnInFront = true,
    ShatterLights = true,
    FlickerLights = {
        Enabled = true,
        Duration = 1
    },
    Cycles = {
        Min = 1,
        Max = 8,
        WaitTime = 2,
    },
    CamShake = {
        Enabled = true,
        Values = {1.2, 20, 0.1,6},
        Range = 100
    },
    ResistCrucifix = true,
    BreakCrucifix = false,
    DeathMessage = {"Hey.. Hm.. Another Entity?..", "A Entity Named A-300..", "I Believes In Interminable Rooms...", "Learn It And Survive It..",},
    IsCuriousLight = true
})

---====== Debug ======---

entity.Debug.OnEntitySpawned = function()
    print("Entity has spawned")
end

entity.Debug.OnEntityDespawned = function()
    print("Entity has despawned")
end

entity.Debug.OnEntityStartMoving = function()
    print("Entity started moving")
end

entity.Debug.OnEntityFinishedRebound = function()
    print("Entity finished rebound")
end

entity.Debug.OnEntityEnteredRoom = function(room)
    print("Entity entered room:", room)
end

entity.Debug.OnLookAtEntity = function()
    print("Player looking at entity")
end

entity.Debug.OnDeath = function()
    print("Player has died")
end

--[[
    NOTE: By overwriting 'OnUseCrucifix', the default crucifixion will be ignored and this function will be called instead

    entity.Debug.OnUseCrucifix = function()
        print("Custom crucifixion script here")
    end
]]--

---====== Run entity ======---

Spawner.runEntity(entity)
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddLabel("Interminable Room's")

Tab:AddButton({
	Name = "A-35",
	Callback = function()
      		---====== Define spawner ======---

local Spawner = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Entity%20Spawner/Source.lua"))()

---====== Create entity ======---

local entity = Spawner.createEntity({
    CustomName = "A-35",
    Model = "rbxassetid/12683218472", -- Your entity's model url here ("rbxassetid/12683218472" or GitHub raw url)
    Speed = 323,
    MoveDelay = 2,
    HeightOffset = 0,
    CanKill = true,
    KillRange = 60,
    SpawnInFront = false,
    ShatterLights = true,
    FlickerLights = {
        Enabled = true,
        Duration = 1
    },
    Cycles = {
        Min = 1,
        Max = 8,
        WaitTime = 2,
    },
    CamShake = {
        Enabled = true,
        Values = {1.2, 20, 0.1,6},
        Range = 100
    },
    ResistCrucifix = true,
    BreakCrucifix = false,
    DeathMessage = {"Hey.. Hm.. Another Entity?..", "A Entity Named A-35..", "I Believes In Interminable Rooms...", "Learn It And Survive It..",},
    IsCuriousLight = true
})

---====== Debug ======---

entity.Debug.OnEntitySpawned = function()
    print("Entity has spawned")
end

entity.Debug.OnEntityDespawned = function()
    print("Entity has despawned")
end

entity.Debug.OnEntityStartMoving = function()
    print("Entity started moving")
end

entity.Debug.OnEntityFinishedRebound = function()
    print("Entity finished rebound")
end

entity.Debug.OnEntityEnteredRoom = function(room)
    print("Entity entered room:", room)
end

entity.Debug.OnLookAtEntity = function()
    print("Player looking at entity")
end

entity.Debug.OnDeath = function()
    print("Player has died")
end

--[[
    NOTE: By overwriting 'OnUseCrucifix', the default crucifixion will be ignored and this function will be called instead

    entity.Debug.OnUseCrucifix = function()
        print("Custom crucifixion script here")
    end
]]--

---====== Run entity ======---

Spawner.runEntity(entity)
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddLabel("Interminable Room's")

Tab:AddButton({
	Name = "Sick A-183",
	Callback = function()
      		---====== Define spawner ======---

local Spawner = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Entity%20Spawner/Source.lua"))()

---====== Create entity ======---

local entity = Spawner.createEntity({
    CustomName = "A-183",
    Model = "rbxassetid://12661668997", -- Your entity's model url here ("rbxassetid://12661668997" or GitHub raw url)
    Speed = 393,
    MoveDelay = 2,
    HeightOffset = 0,
    CanKill = true,
    KillRange = 50,
    SpawnInFront = false,
    ShatterLights = true,
    FlickerLights = {
        Enabled = true,
        Duration = 3.8
    },
    Cycles = {
        Min = 1,
        Max = 18,
        WaitTime = 2.8
    },
    CamShake = {
        Enabled = true,
        Values = {5.5, 10, 0.1, 4},
        Range = 100
    },
    ResistCrucifix = false,
    BreakCrucifix = true,
    DeathMessage = {"You Died To A-183..", "Wait... WHAT!-", "I Wont Even Ask But I Know Who He Is", "Interminable Room's?", "Hes Fast Like One Blink..", "Survive Him.."},
    IsCuriousLight = true
})

---====== Debug ======---

entity.Debug.OnEntitySpawned = function()
    print("Entity has spawned")
end

entity.Debug.OnEntityDespawned = function()
    print("Entity has despawned")
end

entity.Debug.OnEntityStartMoving = function()
    print("Entity started moving")
end

entity.Debug.OnEntityFinishedRebound = function()
    print("Entity finished rebound")
end

entity.Debug.OnEntityEnteredRoom = function(room)
    print("Entity entered room:", room)
end

entity.Debug.OnLookAtEntity = function()
    print("Player looking at entity")
end

entity.Debug.OnDeath = function()
    print("Player has died")
end

--[[
    NOTE: By overwriting 'OnUseCrucifix', the default crucifixion will be ignored and this function will be called instead

    entity.Debug.OnUseCrucifix = function()
        print("Custom crucifixion script here")
    end
]]--

---====== Run entity ======---

Spawner.runEntity(entity)
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

local Tab = Window:MakeTab({
	Name = "~□Hardcore Entities□~",
	Icon = "rbxassetid://11372950109",
	PremiumOnly = false
})

--[[
Name = <string> - The name of the tab.
Icon = <string> - The icon of the tab.
PremiumOnly = <bool> - Makes the tab accessible to Sirus Premium users only.
]]

local Section = Tab:AddSection({
	Name = "Spawn Entities Made By Noonie And Ping!"
})

--[[
Name = <string> - The name of the section.
]]

Tab:AddLabel("Hardcore Mode")

Tab:AddButton({
	Name = "Cease",
	Callback = function()
      		---====== Define spawner ======---

local Spawner = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Entity%20Spawner/Source.lua"))()

---====== Create entity ======---

local entity = Spawner.createEntity({
    CustomName = "Cease",
    Model = "rbxassetid/12262854624", -- Your entity's model url here ("rbxassetid/1265312262854624075397" or GitHub raw url)
    Speed = 76,
    MoveDelay = 2,
    HeightOffset = 0,
    CanKill = true,
    KillRange = 10,
    SpawnInFront = false,
    ShatterLights = true,
    FlickerLights = {
        Enabled = true,
        Duration = 1
    },
    Cycles = {
        Min = 1,
        Max = 10,
        WaitTime = 2,
    },
    CamShake = {
        Enabled = true,
        Values = {1.2, 20, 0.1,6},
        Range = 100
    },
    ResistCrucifix = true,
    BreakCrucifix = false,
    DeathMessage = {"You Died To Cease..", "A Entity From Hardcore Mode..", "I Believe Is From Noonie And Ping..", "Learn It And Survive It..",},
    IsCuriousLight = false
})

---====== Debug ======---

entity.Debug.OnEntitySpawned = function()
    print("Entity has spawned")
end

entity.Debug.OnEntityDespawned = function()
    print("Entity has despawned")
end

entity.Debug.OnEntityStartMoving = function()
    print("Entity started moving")
end

entity.Debug.OnEntityFinishedRebound = function()
    print("Entity finished rebound")
end

entity.Debug.OnEntityEnteredRoom = function(room)
    print("Entity entered room:", room)
end

entity.Debug.OnLookAtEntity = function()
    print("Player looking at entity")
end

entity.Debug.OnDeath = function()
    print("Player has died")
end

--[[
    NOTE: By overwriting 'OnUseCrucifix', the default crucifixion will be ignored and this function will be called instead

    entity.Debug.OnUseCrucifix = function()
        print("Custom crucifixion script here")
    end
]]--

---====== Run entity ======---

Spawner.runEntity(entity)
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddLabel("Hardcore Mode")

Tab:AddButton({
	Name = "Rebound",
	Callback = function()
      		local part = Instance.new("Part")
part.Name = "Bound"
part.Parent = workspace
game.Lighting.MainColorCorrection.TintColor = Color3.fromRGB(61, 171, 98)
game.Lighting.MainColorCorrection.Contrast = 0.2
game.Lighting.MainColorCorrection.Saturation = -0.7
local tween = game:GetService("TweenService")
tween:Create(game.Lighting.MainColorCorrection, TweenInfo.new(5), {Contrast = 0}):Play()
tween:Create(game.Lighting.MainColorCorrection, TweenInfo.new(5), {Saturation = 0}):Play()
local TweenService = game:GetService("TweenService")
local TW = TweenService:Create(game.Lighting.MainColorCorrection, TweenInfo.new(5),{TintColor = Color3.fromRGB(255, 255, 255)})
TW:Play()
local cue1 = Instance.new("Sound")
cue1.Parent = game.Workspace
cue1.Name = "Scream"
cue1.SoundId = "rbxassetid://9114397505"
local distort = Instance.new("DistortionSoundEffect")
distort.Parent = cue1
distort.Level = 1
local distort2 = Instance.new("DistortionSoundEffect")
distort2.Parent = cue1
distort2.Level = 1
local pitch = Instance.new("PitchShiftSoundEffect")
pitch.Parent = cue1
pitch.Octave = 0.5
local pitch2 = Instance.new("PitchShiftSoundEffect")
pitch2.Parent = cue1
pitch2.Octave = 0.5
local pitch3 = Instance.new("PitchShiftSoundEffect")
pitch3.Parent = cue1
pitch3.Octave = 0.5
cue1.Volume = 0.1
cue1:Play()
local cue2 = Instance.new("Sound")
cue2.Parent = game.Workspace
cue2.Name = "Spawn"
cue2.SoundId = "rbxassetid://9114221327"
cue2.Volume = 3
cue2:Play()
local CameraShaker = require(game.ReplicatedStorage.CameraShaker)
local camara = game.Workspace.CurrentCamera
local camShake = CameraShaker.new(Enum.RenderPriority.Camera.Value, function(shakeCf)
	camara.CFrame = camara.CFrame * shakeCf
end)
camShake:Start()
camShake:ShakeOnce(10,3,0.1,6,2,0.5)
wait(2.8)

function GetGitSound(GithubSnd,SoundName)
	local url=GithubSnd
	if not isfile(SoundName..".mp3") then
		writefile(SoundName..".mp3", game:HttpGet(url))
	end
	local sound=Instance.new("Sound")
	sound.SoundId=(getcustomasset or getsynasset)(SoundName..".mp3")
	return sound
end

local scare = Instance.new("Sound")
scare.Parent = game.Workspace
scare.Name = "MyEarsBurn"
scare.SoundId = "rbxassetid://5567523008"
scare.PlaybackSpeed = 3
scare.Volume = 1

local shift = Instance.new("PitchShiftSoundEffect")
shift.Octave = 0.5
shift.Parent = scare

local distort = Instance.new("DistortionSoundEffect")
distort.Parent = scare
distort.Level = 1

local TweenService = game:GetService("TweenService")
local spookee = TweenService:Create(scare, TweenInfo.new(0.3),{Volume = 0})

local Creator = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors%20Entity%20Spawner/Source.lua"))()

-- Create entity

local entity = Creator.createEntity({
    CustomName = "Rebound", -- Custom name of your entity
    Model = "rbxassetid://12254145022", -- Can be GitHub file or rbxassetid
    Speed = 91, -- Percentage, 100 = default Rush speed
    DelayTime = 1.4, -- Time before starting cycles (seconds)
    HeightOffset = 0,
    CanKill = true,
    BreakLights = false,
    KillRange = 40,
    BackwardsMovement = true,
    FlickerLights = {
        false, -- Enabled
        2.5, -- Time (seconds)
    },
    Cycles = {
        Min = 1,
        Max = 1
    },
    CamShake = {
        true, -- Enabled
        {5, 15, 0.1, 1}, -- Shake values (don't change if you don't know)
        100, -- Shake start distance (from Entity to you)
    },
    Jumpscare = {
        false, -- Enabled ('false' if you don't want jumpscare)
        {
            Image1 = "https://www.roblox.com/library/11862656491", -- Image1 url
            Image2 = "https://www.roblox.com/library/11862656491", -- Image2 url
            Shake = false,
            Sound1 = {
                0, -- SoundId
                { Volume = 0.5 }, -- Sound properties
            },
            Sound2 = {
                5567523008, -- SoundId
                { Volume = 0.5 }, -- Sound properties
            },
            Flashing = {
                true, -- Enabled
                Color3.fromRGB(6, 38, 135), -- Color
            },
            Tease = {
                false, -- Enabled ('false' if you don't want tease)
                Min = 1,
                Max = 3,
            },
        },
    },
    CustomDialog = {"You died to Rebound...", "It appears at the next door and has a chance to stay there or run back to the latest door.", "He will come back many times after his initial spawn", "so hide every next door until it is safe.."}, -- Custom death message (can be as long as you want)
})

-----[[ Advanced ]]-----

entity.Debug.OnEntityEnteredRoom = function(room)
    print("Entity:", entity, "has entered room:", room)
    Speed = 0
    wait(2)
    Speed = 170
end

entity.Debug.OnEntitySpawned = function(entityModel)
    print("Entity has spawned:", entityModel)
workspace.Rebound.Rebound_Cue:Destroy()
workspace.Rebound.Rebound_Cue2:Destroy()
workspace.Rebound.Torso.Idle:Destroy()
workspace.Rebound.Torso.Footsteps:Destroy()
local bruh = workspace.Rebound.Torso
local TweenService = game:GetService("TweenService")
local spawn = Instance.new("Sound")
spawn.Parent = bruh
spawn.Name = "ReboundSpawn"
spawn.SoundId = "rbxassetid://9114221327"
spawn.Volume = 5
spawn.RollOffMaxDistance = 10000
spawn.RollOffMinDistance = 450
spawn:Play()
    local move = GetGitSound("https://github.com/check78/worldcuuuup/blob/main/DoomBegin.mp3?raw=true","Reboun")
    move.Parent = bruh
    move.Name = "ReboundMoving"
    move.Volume = 0
    move.Looped = true
local vroom = TweenService:Create(move, TweenInfo.new(2),{Volume = 0.2})
    local distort = Instance.new("DistortionSoundEffect")
    distort.Level = 0.75
    distort.Parent = move
	move.RollOffMaxDistance = 200
	move.RollOffMinDistance = 100
    local tree = Instance.new("TremoloSoundEffect")
    tree.Depth = 1
    tree.Duty = 1
    tree.Frequency = 5
    tree.Parent = move
    local eq = Instance.new("EqualizerSoundEffect")
    eq.HighGain = -60
    eq.MidGain = 10
    eq.LowGain = 10
    eq.Parent = move
	wait(1)
    vroom:Play()
	move:Play()
end

entity.Debug.OnEntityDespawned = function(entityModel)
    print("Entity has despawned:", entityModel)
end

entity.Debug.OnEntityStartMoving = function(entityModel)
    print("Entity has started moving:", entityModel)
end

entity.Debug.OnEntityFinishedRebound = function(entityModel)
    print("Entity finished rebound:", entityModel)
end

entity.Debug.OnDeath = function()
    warn("You died.")

    firesignal(game.ReplicatedStorage.EntityInfo.DeathHint.OnClientEvent, {"You died to Rebound...", "It appears at the next door and has a chance to stay there or run back to the latest door.", "He will come back many times after his initial spawn", "so hide every next door until it is safe.."}, "Blue")

	local ReSt = game.ReplicatedStorage
	local Plr = game.Players.LocalPlayer

		scare:Play()
	    ReSt.GameStats["Player_".. Plr.Name].Total.DeathCause.Value = "Rebound"
		wait(0.5)
		scare:Destroy()

end

------------------------

-- Run the created entity
Creator.runEntity(entity)

local randomnumber = math.random(3,5)
print(randomnumber)

for i = 3,randomnumber do
	game.ReplicatedStorage.GameData.LatestRoom.Changed:Wait()
wait(1)

function GetGitSound(GithubSnd,SoundName)
	local url=GithubSnd
	if not isfile(SoundName..".mp3") then
		writefile(SoundName..".mp3", game:HttpGet(url))
	end
	local sound=Instance.new("Sound")
	sound.SoundId=(getcustomasset or getsynasset)(SoundName..".mp3")
	return sound
end

local scare = Instance.new("Sound")
scare.Parent = game.Workspace
scare.Name = "MyEarsBurn"
scare.SoundId = "rbxassetid://5567523008"
scare.PlaybackSpeed = 3
scare.Volume = 1

local shift = Instance.new("PitchShiftSoundEffect")
shift.Octave = 0.5
shift.Parent = scare

local distort = Instance.new("DistortionSoundEffect")
distort.Parent = scare
distort.Level = 1

local TweenService = game:GetService("TweenService")

local Creator = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors%20Entity%20Spawner/Source.lua"))()

-- Create entity

local entity = Creator.createEntity({
    CustomName = "Rebound", -- Custom name of your entity
    Model = "rbxassetid://12254145022", -- Can be GitHub file or rbxassetid
    Speed = 91, -- Percentage, 100 = default Rush speed
    DelayTime = 1.4, -- Time before starting cycles (seconds)
    HeightOffset = 0,
    CanKill = true,
    BreakLights = false,
    KillRange = 40,
    BackwardsMovement = true,
    FlickerLights = {
        false, -- Enabled
        2.5, -- Time (seconds)
    },
    Cycles = {
        Min = 1,
        Max = 1
    },
    CamShake = {
        true, -- Enabled
        {5, 15, 0.1, 1}, -- Shake values (don't change if you don't know)
        100, -- Shake start distance (from Entity to you)
    },
    Jumpscare = {
        false, -- Enabled ('false' if you don't want jumpscare)
        {
            Image1 = "https://www.roblox.com/library/11862656491", -- Image1 url
            Image2 = "https://www.roblox.com/library/11862656491", -- Image2 url
            Shake = false,
            Sound1 = {
                0, -- SoundId
                { Volume = 0.5 }, -- Sound properties
            },
            Sound2 = {
                5567523008, -- SoundId
                { Volume = 0.5 }, -- Sound properties
            },
            Flashing = {
                true, -- Enabled
                Color3.fromRGB(6, 38, 135), -- Color
            },
            Tease = {
                false, -- Enabled ('false' if you don't want tease)
                Min = 1,
                Max = 3,
            },
        },
    },
    CustomDialog = {"You died to Rebound...", "It appears at the next door and has a chance to stay there or run back to the latest door.", "He will come back many times after his initial spawn", "so hide every next door until it is safe.."}, -- Custom death message (can be as long as you want)
})

-----[[ Advanced ]]-----

entity.Debug.OnEntityEnteredRoom = function(room)
    print("Entity:", entity, "has entered room:", room)
    Speed = 0
    wait(2)
    Speed = 170
end

entity.Debug.OnEntitySpawned = function(entityModel)
    print("Entity has spawned:", entityModel)
workspace.Rebound.Rebound_Cue:Destroy()
workspace.Rebound.Rebound_Cue2:Destroy()
workspace.Rebound.Torso.Idle:Destroy()
workspace.Rebound.Torso.Footsteps:Destroy()
local bruh = workspace.Rebound.Torso
local TweenService = game:GetService("TweenService")
local spawn = Instance.new("Sound")
spawn.Parent = bruh
spawn.Name = "ReboundSpawn"
spawn.SoundId = "rbxassetid://9114221327"
spawn.Volume = 5
spawn.RollOffMaxDistance = 10000
spawn.RollOffMinDistance = 450
spawn:Play()
    local move = GetGitSound("https://github.com/check78/worldcuuuup/blob/main/DoomBegin.mp3?raw=true","Reboun")
    move.Parent = bruh
    move.Name = "ReboundMoving"
    move.Volume = 0
    move.Looped = true
local vroom = TweenService:Create(move, TweenInfo.new(2),{Volume = 0.2})
    local distort = Instance.new("DistortionSoundEffect")
    distort.Level = 0.75
    distort.Parent = move
	move.RollOffMaxDistance = 200
	move.RollOffMinDistance = 100
    local tree = Instance.new("TremoloSoundEffect")
    tree.Depth = 1
    tree.Duty = 1
    tree.Frequency = 5
    tree.Parent = move
    local eq = Instance.new("EqualizerSoundEffect")
    eq.HighGain = -60
    eq.MidGain = 10
    eq.LowGain = 10
    eq.Parent = move
	wait(1)
    vroom:Play()
	move:Play()
end

entity.Debug.OnEntityDespawned = function(entityModel)
    print("Entity has despawned:", entityModel)
end

entity.Debug.OnEntityStartMoving = function(entityModel)
    print("Entity has started moving:", entityModel)
end

entity.Debug.OnEntityFinishedRebound = function(entityModel)
    print("Entity finished rebound:", entityModel)
end

entity.Debug.OnDeath = function()
    warn("You died.")

    firesignal(game.ReplicatedStorage.EntityInfo.DeathHint.OnClientEvent, {"You died to Rebound...", "It appears at the next door and has a chance to stay there or run back to the latest door.", "He will come back many times after his initial spawn", "so hide every next door until it is safe.."}, "Blue")

	local ReSt = game.ReplicatedStorage
	local Plr = game.Players.LocalPlayer

		scare:Play()
	    ReSt.GameStats["Player_".. Plr.Name].Total.DeathCause.Value = "Rebound"
		wait(0.5)
		scare:Destroy()

end

------------------------

-- Run the created entity
Creator.runEntity(entity)
end
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddLabel("Hardcore Mode")

Tab:AddButton({
	Name = "Ripper",
	Callback = function()
---====== Define spawner ======---

local Spawner = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Entity%20Spawner/Source.lua"))()

---====== Create entity ======---

local entity = Spawner.createEntity({
    CustomName = "Ripper",
    Model = "rbxassetid/12262768551", -- Your entity's model url here ("rbxassetid/12262768551" or GitHub raw url)
    Speed = 107,
    MoveDelay = 2,
    HeightOffset = 0,
    CanKill = true,
    KillRange = 50,
    SpawnInFront = false,
    ShatterLights = false,
    FlickerLights = {
        Enabled = true,
        Duration = 4
    },
    Cycles = {
        Min = 1,
        Max = 1,
        Delay = 2,
    },
    CamShake = {
        Enabled = true,
        Values = {2.3, 20, 0.3,2},
        Range = 100
    },
    ResistCrucifix = false,
    BreakCrucifix = true,
    DeathMessage = {"You died to who you call Ripper...", "You can tell his presence by the lights and scream", "Hide when he does this!"},
    IsCuriousLight = false
})

---====== Debug ======---

entity.Debug.OnEntitySpawned = function()
    print("Entity has spawned")
end

entity.Debug.OnEntityDespawned = function()
    print("Entity has despawned")
end

entity.Debug.OnEntityStartMoving = function()
    print("Entity started moving")
end

entity.Debug.OnEntityFinishedRebound = function()
    print("Entity finished rebound")
end

entity.Debug.OnEntityEnteredRoom = function(room)
    print("Entity entered room:", room)
end

entity.Debug.OnLookAtEntity = function()
    print("Player looking at entity")
end

entity.Debug.OnDeath = function()
    print("Player has died")
end

--[[
    NOTE: By overwriting 'OnUseCrucifix', the default crucifixion will be ignored and this function will be called instead

    entity.Debug.OnUseCrucifix = function()
        print("Custom crucifixion script here")
    end
]]--

---====== Run entity ======---

Spawner.runEntity(entity)
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddLabel("Hardcore Mode")

Tab:AddButton({
	Name = "Deer God",
	Callback = function()
      		---====== Define spawner ======---

local Spawner = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Entity%20Spawner/Source.lua"))()

---====== Create entity ======---

local entity = Spawner.createEntity({
    CustomName = "Deer God",
    Model = "rbxassetid/12262883448", -- Your entity's model url here ("rbxassetid/12262883448" or GitHub raw url)
    Speed = 51,
    MoveDelay = 2,
    HeightOffset = 0,
    CanKill = false,
    KillRange = 50,
    SpawnInFront = false,
    ShatterLights = true,
    FlickerLights = {
        Enabled = true,
        Duration = 4.9
    },
    Cycles = {
        Min = 1,
        Max = 1,
        Delay = 2,
    },
    CamShake = {
        Enabled = true,
        Values = {9.8, 20, 0.4,7},
        Range = 100
    },
    ResistCrucifix = true,
    BreakCrucifix = false,
    DeathMessage = {"Oh God...", "Its Deer God...", "I Believe In Hardcore Mode...", "Learn It And Survive It..",},
    IsCuriousLight = true
})

---====== Debug ======---

entity.Debug.OnEntitySpawned = function()
    print("Entity has spawned")
end

entity.Debug.OnEntityDespawned = function()
    print("Entity has despawned")
end

entity.Debug.OnEntityStartMoving = function()
    print("Entity started moving")
end

entity.Debug.OnEntityFinishedRebound = function()
    print("Entity finished rebound")
end

entity.Debug.OnEntityEnteredRoom = function(room)
    print("Entity entered room:", room)
end

entity.Debug.OnLookAtEntity = function()
    print("Player looking at entity")
end

entity.Debug.OnDeath = function()
    print("Player has died")
end

--[[
    NOTE: By overwriting 'OnUseCrucifix', the default crucifixion will be ignored and this function will be called instead

    entity.Debug.OnUseCrucifix = function()
        print("Custom crucifixion script here")
    end
]]--

---====== Run entity ======---

Spawner.runEntity(entity)
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddLabel("Hardcore Mode")

Tab:AddButton({
	Name = "Dread",
	Callback = function()
      		---====== Define spawner ======---

local Spawner = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Entity%20Spawner/Source.lua"))()

---====== Create entity ======---

local entity = Spawner.createEntity({
    CustomName = "Dread",
    Model = "rbxassetid://12654337720", -- Your entity's model url here ("rbxassetid://1234567890" or GitHub raw url)
    Speed = 100,
    MoveDelay = 2,
    HeightOffset = 0,
    CanKill = true,
    KillRange = 50,
    SpawnInFront = false,
    ShatterLights = true,
    FlickerLights = {
        Enabled = true,
        Duration = 2.3
    },
    Cycles = {
        Min = 1,
        Max = 1,
        Delay = 2
    },
    CamShake = {
        Enabled = true,
        Values = {1.5, 20, 0.1, 1},
        Range = 100
    },
    ResistCrucifix = false,
    BreakCrucifix = true,
    DeathMessage = {"YOU DIED TO DREAD AGAIN?", "i swear.. I WILL BAN YOU BECAUSE DREAD IS JUST EASY", "HES LIKE RUSH!", "I HATE YOU SO MUCH IDIOT!-", "... i- im sorry sus", "forgive me please i was just mad..", "please..", "thanks for the hug.."},
    IsCuriousLight = false
})

---====== Debug ======---

entity.Debug.OnEntitySpawned = function()
    print("Entity has spawned")
end

entity.Debug.OnEntityDespawned = function()
    print("Entity has despawned")
end

entity.Debug.OnEntityStartMoving = function()
    print("Entity started moving")
end

entity.Debug.OnEntityFinishedRebound = function()
    print("Entity finished rebound")
end

entity.Debug.OnEntityEnteredRoom = function(room)
    print("Entity entered room:", room)
end

entity.Debug.OnLookAtEntity = function()
    print("Player looking at entity")
end

entity.Debug.OnDeath = function()
    print("Player has died")
end

--[[
    NOTE: By overwriting 'OnUseCrucifix', the default crucifixion will be ignored and this function will be called instead

    entity.Debug.OnUseCrucifix = function()
        print("Custom crucifixion script here")
    end
]]--

---====== Run entity ======---

Spawner.runEntity(entity)
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddLabel("Hardcore Mode")

Tab:AddButton({
	Name = "Shocker",
	Callback = function()
      		firesignal(game.ReplicatedStorage.Bricks.DeathHint.OnClientEvent, {"You Died To Shocker...", "When He Appears, Do Not Look At Him!", "Hiding Wont Work So, Just Dont Look At Him"})    

	local killed = false
	local ReSt = game:GetService("ReplicatedStorage")
	local Plr = game.Players.LocalPlayer
	local val = 80
	local events = require(game.ReplicatedStorage.ClientModules.Module_Events)
	local cameraShaker = require(game.ReplicatedStorage.CameraShaker)
	local camera = workspace.CurrentCamera

	local camShake = cameraShaker.new(Enum.RenderPriority.Camera.Value, function(cf)
		camera.CFrame = camera.CFrame * cf
	end)
	camShake:Start()
	function GetTime(Distance, Speed)
		-- Time = Distance / Speed
		local Time = Distance / Speed
		return Time
	end
	function GetGitSound(GithubSnd,SoundName)
		local url=GithubSnd
		if not isfile(SoundName..".mp3") then
			writefile(SoundName..".mp3", game:HttpGet(url))
		end
		local sound=Instance.new("Sound")
		sound.SoundId=(getcustomasset or getsynasset)(SoundName..".mp3")
		return sound
	end
	function GetGitModel(ModelUrl,ModelName)
		if not isfile(ModelName..".txt") then writefile(ModelName..".txt", game:HttpGet(ModelUrl)) end
		local a=game:GetObjects((getcustomasset or getsynasset)(ModelName..".txt"))[1]
		a.Name=ModelName
		return a
	end

	local Model = game:GetObjects("rbxassetid://11547601187")[1]      ---11547601187
	Model.Parent = workspace
	local Shocker = Model:FindFirstChildWhichIsA("BasePart")
	function IsScreen()
		local isOnScreen = select(2, camera:WorldToViewportPoint(Shocker.Position));
		if isOnScreen then
			return true
		end
	end
	local dead = false

	local offset = Vector3.new(0,0,-math.random(5,20))
	Shocker.CFrame = Plr.Character.HumanoidRootPart.CFrame*CFrame.new(offset)
	Shocker.PlaySound:Play()

	spawn(function()
		wait(2)
		if IsScreen() then
			dead = true
		end
	end)

	repeat wait() until dead == true or not IsScreen()
	if dead == true then
		spawn(function()
			while dead do wait()
				if Plr.Character:FindFirstChildWhichIsA("Humanoid") then
					Plr.Character:FindFirstChildWhichIsA("Humanoid").WalkSpeed = 15
				end
			end
		end)
		Shocker["HORROR SCREAM 15"]:Play()
		game.TweenService:Create(Shocker,TweenInfo.new(0.4,Enum.EasingStyle.Circular,Enum.EasingDirection.In),{CFrame = Plr.Character.HumanoidRootPart.CFrame}):Play()
		wait(0.4)
		Plr.Character:FindFirstChildWhichIsA("Humanoid"):TakeDamage(0)
		camShake:Shake(cameraShaker.Presets.Explosion)
		ReSt.GameStats["Player_".. Plr.Name].Total.DeathCause.Value = "Shocker"
		firesignal(game.ReplicatedStorage.Bricks.DeathHint.OnClientEvent, {"You died to who you call Shocker..","Dont look at it or it stuns you!"})
		game.TweenService:Create(Shocker,TweenInfo.new(0.4,Enum.EasingStyle.Circular,Enum.EasingDirection.In),{CFrame = Shocker.CFrame + Vector3.new(0,-10,0)}):Play()
		game.TweenService:Create(Shocker.PlaySound,TweenInfo.new(1,Enum.EasingStyle.Circular,Enum.EasingDirection.In),{Volume = 0}):Play()
		wait(1)
		Shocker:Destroy()
		wait(2)
		dead = false
	end

	if dead	== false then
		if gotshocker == false then gotshocker = true 
			getgenv().Title = "Shocking Experience" --Title Here
			getgenv().Description = "Look at me." --Description Here
			getgenv().Reason = "Encounter Shocker." --Reason Here
			getgenv().BadgeId = 2129271052 --Replace Number with Your Badge ID
			getgenv().Category = 10 --You can replace the Category or dont

			local Unlock = require(game.Players.LocalPlayer.PlayerGui.MainUI.Initiator.Main_Lobby.RemoteListener.Modules.AchievementUnlock)
			local Achievements = debug.getupvalue(Unlock, 1)
			for i,v in pairs(require(game:GetService("ReplicatedStorage").Achievements)) do
				v.Title = getgenv().Title
				v.Desc = getgenv().Description
				v.Reason = getgenv().Reason
				v.BadgeId = getgenv().BadgeId
				v.Category = getgenv().Category
			end
			spawn(function()
				Unlock(nil,"Join")
			end)
		end
		game.TweenService:Create(Shocker,TweenInfo.new(0.4,Enum.EasingStyle.Circular,Enum.EasingDirection.In),{CFrame = Shocker.CFrame + Vector3.new(0,-20,0)}):Play()
		game.TweenService:Create(Shocker.PlaySound,TweenInfo.new(1,Enum.EasingStyle.Circular,Enum.EasingDirection.In),{Volume = 0}):Play()
		wait(1)
		Shocker:Destroy()
	end
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

local Tab = Window:MakeTab({
	Name = "◇Door's Entities◇",
	Icon = "rbxassetid://7734068321",
	PremiumOnly = false
})

--[[
Name = <string> - The name of the tab.
Icon = <string> - The icon of the tab.
PremiumOnly = <bool> - Makes the tab accessible to Sirus Premium users only.
]]

local Section = Tab:AddSection({
	Name = "Spawns Entities Made In Doors!"
})

--[[
Name = <string> - The name of the section.
]]

Tab:AddLabel("Doors")

Tab:AddButton({
	Name = "Void",
	Callback = function()
      		local a = require(game.ReplicatedStorage.ClientModules.EntityModules.Void).stuff(require(game.Players.LocalPlayer.PlayerGui.MainUI.Initiator.Main_Game),
workspace.CurrentRooms[game.Players.LocalPlayer:GetAttribute("CurrentRoom")])
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddLabel("Doors")

Tab:AddButton({
	Name = "Seek Eyes",
	Callback = function()
      		local a = game.Players.LocalPlayer.PlayerGui.MainUI.Initiator.Main_Game
require(game.ReplicatedStorage.ClientModules.EntityModules.Seek).tease(nil, workspace.CurrentRooms[game.Players.LocalPlayer:GetAttribute("CurrentRoom")], 100)
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddLabel("Doors")

Tab:AddButton({
	Name = "Halt",
	Callback = function()
      		require(game.ReplicatedStorage.ClientModules.EntityModules.Shade).stuff(require(game.Players.LocalPlayer.PlayerGui.MainUI.Initiator.Main_Game),
workspace.CurrentRooms[game.Players.LocalPlayer:GetAttribute("CurrentRoom")])
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddLabel("Doors")

Tab:AddButton({
	Name = "Screech",
	Callback = function()
      		require(game.StarterGui.MainUI.Initiator.Main_Game.RemoteListener.Modules.Screech)(require(game.Players.LocalPlayer.PlayerGui.MainUI.Initiator.Main_Game),
    workspace.CurrentRooms[game.Players.LocalPlayer:GetAttribute("CurrentRoom")])
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddLabel("Doors")

Tab:AddButton({
	Name = "Eyes",
	Callback = function()
      		local EntitySpawner = loadstring(game:HttpGet("https://raw.githubusercontent.com/dreadmania/Scripts/main/Spawner_V2.lua"))()
local Configuration = {
    Kill = true, -- change to "Damage = 10," for eyes, doesnt work on other entities
    Speed = 160, -- 60 for rush, doesnt work on other entities
    Time = 3 -- 5 for rush, doesnt work on other entities
}

EntitySpawner:Spawn("Eyes", Configuration)
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddLabel("Doors")

Tab:AddButton({
	Name = "Timothy",
	Callback = function()
      		local a = game.Players.LocalPlayer.PlayerGui.MainUI.Initiator.Main_Game
require(a.RemoteListener.Modules.SpiderJumpscare)(require(a), workspace.CurrentRooms["0"].Assets.Dresser.DrawerContainer, 0.2)
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddLabel("Doors")

Tab:AddButton({
	Name = "Glitch",
	Callback = function()
      		require(game.ReplicatedStorage.ClientModules.EntityModules.Glitch).stuff(require(game.Players.LocalPlayer.PlayerGui.MainUI.Initiator.Main_Game),
workspace.CurrentRooms[game.Players.LocalPlayer:GetAttribute("CurrentRoom")])
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddLabel("Doors")

Tab:AddButton({
	Name = "Rush [KILLABLE | JUMPSCARE]",
	Callback = function()
      		local Creator = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors%20Entity%20Spawner/Source.lua"))()

-- Create entity

local entity = Creator.createEntity({
    CustomName = "Rush", -- Custom name of your entity
    Model = "https://github.com/RegularVynixu/Utilities/blob/main/Doors%20Entity%20Spawner/Models/Rush.rbxm?raw=true", -- Can be GitHub file or rbxassetid
    Speed = 100, -- Percentage, 100 = default Rush speed
    DelayTime = 2, -- Time before starting cycles (seconds)
    HeightOffset = 0,
    CanKill = true,
    BreakLights = true,
    KillRange = 50,
    BackwardsMovement = false,
    FlickerLights = {
        true, -- Enabled
        1.2, -- Time (seconds)
    },
    Cycles = {
        Min = 1,
        Max = 1,
        WaitTime = 4,
    },
    CamShake = {
        true, -- Enabled
        {5, 15, 0.1, 1}, -- Shake values (don't change if you don't know)
        100, -- Shake start distance (from Entity to you)
    },
    Jumpscare = {
        true, -- Enabled/Disabled
        {
            Image1 = "rbxassetid://10483855823", -- Image1 url
            Image2 = "rbxassetid://10483999903", -- Image2 url
            Shake = true,
            Sound1 = {
                10483790459, -- SoundId
                { Volume = 0.5 }, -- Sound properties
            },
            Sound2 = {
                10483837590, -- SoundId
                { Volume = 0.5 }, -- Sound properties
            },
            Flashing = {
                true, -- Enabled/Disabled
                Color3.fromRGB(255, 255, 255), -- Color
            },
            Tease = {
                true, -- Enabled/Disabled
                Min = 1,
                Max = 3,
            },
        },
    },
    CustomDialog = {"It seems you are having trouble with Rush...", "The lights will always flicker when it is near.", "Whenever this happens, find a hiding spot!"}, -- Custom death message (can be as long as you want)
})

-----[[ Advanced ]]-----

entity.Debug.OnEntityEnteredRoom = function(room)
    print("Entity:", entity, "has entered room:", room)
    Speed = 0
    wait(2)
    Speed = 100
end

entity.Debug.OnEntityDespawned = function(entityModel)
    print("Entity has despawned:", entityModel)
end

entity.Debug.OnEntityStartMoving = function(entityModel)
    print("Entity has started moving:", entityModel)
end

entity.Debug.OnEntityFinishedRebound = function(entityModel)
    print("Entity finished rebound:", entityModel)
end

entity.Debug.OnDeath = function()
    warn("You died.")

    firesignal(game.ReplicatedStorage.EntityInfo.DeathHint.OnClientEvent, {"It seems you are having trouble with Rush...", "The lights will always flicker when it is near.", "Whenever this happens, find a hiding spot!"}, "Blue")

	local ReSt = game.ReplicatedStorage
	local Plr = game.Players.LocalPlayer

		scare:Play()
	    ReSt.GameStats["Player_".. Plr.Name].Total.DeathCause.Value = "Rush"
		wait(0.5)
		scare:Destroy()

end

------------------------

-- Run the created entity
Creator.runEntity(entity)
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddButton({
	Name = "Rush",
	Callback = function()
      		local Creator = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors%20Entity%20Spawner/Source.lua"))()

-- Create entity

local entity = Creator.createEntity({
    CustomName = "Rush", -- Custom name of your entity
    Model = "https://github.com/RegularVynixu/Utilities/blob/main/Doors%20Entity%20Spawner/Models/Rush.rbxm?raw=true", -- Can be GitHub file or rbxassetid
    Speed = 100, -- Percentage, 100 = default Rush speed
    DelayTime = 2, -- Time before starting cycles (seconds)
    HeightOffset = 0,
    CanKill = false,
    BreakLights = true,
    KillRange = 50,
    BackwardsMovement = false,
    FlickerLights = {
        true, -- Enabled
        1.2, -- Time (seconds)
    },
    Cycles = {
        Min = 1,
        Max = 1,
        WaitTime = 4,
    },
    CamShake = {
        true, -- Enabled
        {5, 15, 0.1, 1}, -- Shake values (don't change if you don't know)
        100, -- Shake start distance (from Entity to you)
    },
    Jumpscare = {
        true, -- Enabled/Disabled
        {
            Image1 = "rbxassetid://10483855823", -- Image1 url
            Image2 = "rbxassetid://10483999903", -- Image2 url
            Shake = true,
            Sound1 = {
                10483790459, -- SoundId
                { Volume = 0.5 }, -- Sound properties
            },
            Sound2 = {
                10483837590, -- SoundId
                { Volume = 0.5 }, -- Sound properties
            },
            Flashing = {
                true, -- Enabled/Disabled
                Color3.fromRGB(255, 255, 255), -- Color
            },
            Tease = {
                true, -- Enabled/Disabled
                Min = 1,
                Max = 3,
            },
        },
    },
    CustomDialog = {"It seems you are having trouble with Rush...", "The lights will always flicker when it is near.", "Whenever this happens, find a hiding spot!"}, -- Custom death message (can be as long as you want)
})

-----[[ Advanced ]]-----

entity.Debug.OnEntityEnteredRoom = function(room)
    print("Entity:", entity, "has entered room:", room)
    Speed = 0
    wait(2)
    Speed = 100
end

entity.Debug.OnEntityDespawned = function(entityModel)
    print("Entity has despawned:", entityModel)
end

entity.Debug.OnEntityStartMoving = function(entityModel)
    print("Entity has started moving:", entityModel)
end

entity.Debug.OnEntityFinishedRebound = function(entityModel)
    print("Entity finished rebound:", entityModel)
end

entity.Debug.OnDeath = function()
    warn("You died.")

    firesignal(game.ReplicatedStorage.EntityInfo.DeathHint.OnClientEvent, {"It seems you are having trouble with Rush...", "The lights will always flicker when it is near.", "Whenever this happens, find a hiding spot!"}, "Blue")

	local ReSt = game.ReplicatedStorage
	local Plr = game.Players.LocalPlayer

		scare:Play()
	    ReSt.GameStats["Player_".. Plr.Name].Total.DeathCause.Value = "Rush"
		wait(0.5)
		scare:Destroy()

end

------------------------

-- Run the created entity
Creator.runEntity(entity)
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddLabel("Doors")

Tab:AddButton({
	Name = "Ambush [KILLABLE]",
	Callback = function()
	local Creator = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors%20Entity%20Spawner/Source.lua"))()

-- Create entity

local entity = Creator.createEntity({
    CustomName = "Ambush", -- Custom name of your entity
    Model = "https://github.com/RegularVynixu/Utilities/blob/main/Doors%20Entity%20Spawner/Models/Ambush.rbxm?raw=true", -- Can be GitHub file or rbxassetid
    Speed = 289, -- Percentage, 100 = default Rush speed
    DelayTime = 2, -- Time before starting cycles (seconds)
    HeightOffset = 0,
    CanKill = true,
    BreakLights = false,
    KillRange = 100,
    BackwardsMovement = false,
    FlickerLights = {
        true, -- Enabled
        1.3, -- Time (seconds)
    },
    Cycles = {
        Min = 1,
        Max = 6,
        WaitTime = 3,
    },
    CamShake = {
        true, -- Enabled
        {5, 15, 0.1, 1}, -- Shake values (don't change if you don't know)
        100, -- Shake start distance (from Entity to you)
    },
    Jumpscare = {
        false, -- Enabled/Disabled
        {
            Image1 = "rbxassetid://10483855823", -- Image1 url
            Image2 = "rbxassetid://10483999903", -- Image2 url
            Shake = true,
            Sound1 = {
                10483790459, -- SoundId
                { Volume = 0.5 }, -- Sound properties
            },
            Sound2 = {
                10483837590, -- SoundId
                { Volume = 0.5 }, -- Sound properties
            },
            Flashing = {
                true, -- Enabled/Disabled
                Color3.fromRGB(255, 255, 255), -- Color
            },
            Tease = {
                true, -- Enabled/Disabled
                Min = 1,
                Max = 3,
            },
        },
    },
    CustomDialog = {"You died to Ambush.", "You might need to hop in and out of your hiding spot a couple times."}, -- Custom death message (can be as long as you want)
})

-----[[ Advanced ]]-----

entity.Debug.OnEntityEnteredRoom = function(room)
    print("Entity:", entity, "has entered room:", room)
    Speed = 0
    wait(2)
    Speed = 100
end

entity.Debug.OnEntityDespawned = function(entityModel)
    print("Entity has despawned:", entityModel)
end

entity.Debug.OnEntityStartMoving = function(entityModel)
    print("Entity has started moving:", entityModel)
end

entity.Debug.OnEntityFinishedRebound = function(entityModel)
    print("Entity finished rebound:", entityModel)
end

entity.Debug.OnDeath = function()
    warn("You died.")

    firesignal(game.ReplicatedStorage.EntityInfo.DeathHint.OnClientEvent, {"You died to Ambush.", "You might need to hop in and out of your hiding spot a couple times."}, "Blue")

	local ReSt = game.ReplicatedStorage
	local Plr = game.Players.LocalPlayer

		scare:Play()
	    ReSt.GameStats["Player_".. Plr.Name].Total.DeathCause.Value = "Ambush"
		wait(0.5)
		scare:Destroy()

end

------------------------

-- Run the created entity
Creator.runEntity(entity)
	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddButton({
	Name = "Ambush",
	Callback = function()
	local Creator = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors%20Entity%20Spawner/Source.lua"))()

-- Create entity

local entity = Creator.createEntity({
    CustomName = "Ambush", -- Custom name of your entity
    Model = "https://github.com/RegularVynixu/Utilities/blob/main/Doors%20Entity%20Spawner/Models/Ambush.rbxm?raw=true", -- Can be GitHub file or rbxassetid
    Speed = 289, -- Percentage, 100 = default Rush speed
    DelayTime = 2, -- Time before starting cycles (seconds)
    HeightOffset = 0,
    CanKill = false,
    BreakLights = false,
    KillRange = 100,
    BackwardsMovement = false,
    FlickerLights = {
        true, -- Enabled
        1.3, -- Time (seconds)
    },
    Cycles = {
        Min = 1,
        Max = 6,
        WaitTime = 3,
    },
    CamShake = {
        true, -- Enabled
        {5, 15, 0.1, 1}, -- Shake values (don't change if you don't know)
        100, -- Shake start distance (from Entity to you)
    },
    Jumpscare = {
        false, -- Enabled/Disabled
        {
            Image1 = "rbxassetid://10483855823", -- Image1 url
            Image2 = "rbxassetid://10483999903", -- Image2 url
            Shake = true,
            Sound1 = {
                10483790459, -- SoundId
                { Volume = 0.5 }, -- Sound properties
            },
            Sound2 = {
                10483837590, -- SoundId
                { Volume = 0.5 }, -- Sound properties
            },
            Flashing = {
                true, -- Enabled/Disabled
                Color3.fromRGB(255, 255, 255), -- Color
            },
            Tease = {
                true, -- Enabled/Disabled
                Min = 1,
                Max = 3,
            },
        },
    },
    CustomDialog = {"You died to Ambush.", "You might need to hop in and out of your hiding spot a couple times."}, -- Custom death message (can be as long as you want)
})

-----[[ Advanced ]]-----

entity.Debug.OnEntityEnteredRoom = function(room)
    print("Entity:", entity, "has entered room:", room)
    Speed = 0
    wait(2)
    Speed = 100
end

entity.Debug.OnEntityDespawned = function(entityModel)
    print("Entity has despawned:", entityModel)
end

entity.Debug.OnEntityStartMoving = function(entityModel)
    print("Entity has started moving:", entityModel)
end

entity.Debug.OnEntityFinishedRebound = function(entityModel)
    print("Entity finished rebound:", entityModel)
end

entity.Debug.OnDeath = function()
    warn("You died.")

    firesignal(game.ReplicatedStorage.EntityInfo.DeathHint.OnClientEvent, {"You died to Ambush.", "You might need to hop in and out of your hiding spot a couple times."}, "Blue")

	local ReSt = game.ReplicatedStorage
	local Plr = game.Players.LocalPlayer

		scare:Play()
	    ReSt.GameStats["Player_".. Plr.Name].Total.DeathCause.Value = "Ambush"
		wait(0.5)
		scare:Destroy()

end

------------------------

-- Run the created entity
Creator.runEntity(entity)
	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

local Tab = Window:MakeTab({
	Name = "●Custom Door's Entities●",
	Icon = "rbxassetid://11372950109",
	PremiumOnly = false
})

--[[
Name = <string> - The name of the tab.
Icon = <string> - The icon of the tab.
PremiumOnly = <bool> - Makes the tab accessible to Sirus Premium users only.
]]

local Section = Tab:AddSection({
	Name = "Spawns Entities That Are Custom!"
})

--[[
Name = <string> - The name of the section.
]]

Tab:AddButton({
	Name = "Threat",
	Callback = function()
	local Creator = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors%20Entity%20Spawner/Source.lua"))()

-- Create entity

local entity = Creator.createEntity({
    CustomName = "Threat", -- Custom name of your entity
    Model = "https://github.com/HollowedOutMods/Doors/blob/main/threat.rbxm?raw=true", -- Can be GitHub file or rbxassetid
    Speed = 367, -- Percentage, 100 = default Rush speed
    DelayTime = 2, -- Time before starting cycles (seconds)
    HeightOffset = 0,
    CanKill = true,
    BreakLights = true,
    KillRange = 50,
    BackwardsMovement = false,
    FlickerLights = {
        true, -- Enabled
        14, -- Time (seconds)
    },
    Cycles = {
        Min = 1,
        Max = 20,
        WaitTime = 10,
    },
    CamShake = {
        true, -- Enabled
        {6, 20, 0.1, 1}, -- Shake values (don't change if you don't know)
        200, -- Shake start distance (from Entity to you)
    },
    Jumpscare = {
        false, -- Enabled/Disabled
        {
            Image1 = "rbxassetid://10483855823", -- Image1 url
            Image2 = "rbxassetid://10483999903", -- Image2 url
            Shake = true,
            Sound1 = {
                10483790459, -- SoundId
                { Volume = 0.5 }, -- Sound properties
            },
            Sound2 = {
                10483837590, -- SoundId
                { Volume = 0.5 }, -- Sound properties
            },
            Flashing = {
                true, -- Enabled/Disabled
                Color3.fromRGB(255, 255, 255), -- Color
            },
            Tease = {
                true, -- Enabled/Disabled
                Min = 1,
                Max = 3,
            },
        },
    },
    CustomDialog = {"You died to Threat.", "Its kinda hard to survive it."}, -- Custom death message (can be as long as you want)
})

-----[[ Advanced ]]-----

entity.Debug.OnEntityEnteredRoom = function(room)
    print("Entity:", entity, "has entered room:", room)
    Speed = 0
    wait(2)
    Speed = 100
end

entity.Debug.OnEntityDespawned = function(entityModel)
    print("Entity has despawned:", entityModel)
end

entity.Debug.OnEntityStartMoving = function(entityModel)
    print("Entity has started moving:", entityModel)
end

entity.Debug.OnEntityFinishedRebound = function(entityModel)
    print("Entity finished rebound:", entityModel)
end

entity.Debug.OnDeath = function()
    warn("You died.")

    firesignal(game.ReplicatedStorage.EntityInfo.DeathHint.OnClientEvent, {"You died to Threat.", "Its kinda hard to survive it."}, "Blue")

	local ReSt = game.ReplicatedStorage
	local Plr = game.Players.LocalPlayer

		scare:Play()
	    ReSt.GameStats["Player_".. Plr.Name].Total.DeathCause.Value = "Threat"
		wait(0.5)
		scare:Destroy()

end

------------------------

-- Run the created entity
Creator.runEntity(entity)
	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddLabel("Custom Entity")

Tab:AddButton({
	Name = "-<Depth>-",
	Callback = function()
---====== Define spawner ======---

local Spawner = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Entity%20Spawner/Source.lua"))()

---====== Create entity ======---

local entity = Spawner.createEntity({
    CustomName = "Depth",
    Model = "rbxassetid://12453520636", -- Your entity's model url here ("rbxassetid://1234567890" or GitHub raw url)
    Speed = 300,
    MoveDelay = 2,
    HeightOffset = 0,
    CanKill = true,
    KillRange = 40,
    SpawnInFront = false,
    ShatterLights = true,
    FlickerLights = {
        Enabled = true,
        Duration = 15
    },
    Cycles = {
        Min = 2,
        Max = 23,
        WaitTime = 2
    },
    CamShake = {
        Enabled = true,
        Values = {1.9, 30, 0.1, 1},
        Range = 100
    },
    ResistCrucifix = false,
    BreakCrucifix = false,
    DeathMessage = {"You Died To-", "Wait.. WHAT DEPTH?!?", "OMG!?!", "IMA JUST SAY IT REBOUNDS 23 TIMES", "IM.. SHOCKED."},
    IsCuriousLight = false
})

---====== Debug ======---

entity.Debug.OnEntitySpawned = function()
    print("Entity has spawned")
end

entity.Debug.OnEntityDespawned = function()
    print("Entity has despawned")
end

entity.Debug.OnEntityStartMoving = function()
    print("Entity started moving")
end

entity.Debug.OnEntityFinishedRebound = function()
    print("Entity finished rebound")
end

entity.Debug.OnEntityEnteredRoom = function(room)
    print("Entity entered room:", room)
end

entity.Debug.OnLookAtEntity = function()
    print("Player looking at entity")
end

entity.Debug.OnDeath = function()
    print("Player has died")
end

--[[
    NOTE: By overwriting 'OnUseCrucifix', the default crucifixion will be ignored and this function will be called instead

    entity.Debug.OnUseCrucifix = function()
        print("Custom crucifixion script here")
    end
]]--

---====== Run entity ======---

Spawner.runEntity(entity)
	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddLabel("Custom Entity")

Tab:AddButton({
	Name = "Screaming Ambush",
	Callback = function()
      		local Spawner = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors%20Entity%20Spawner/Source.lua"))()


-- Create entity
local entityTable = Spawner.createEntity({
    CustomName = "Screaming ambush", -- Custom name of your entity
    Model = "rbxassetid://12607852340", -- Can be GitHub file or rbxassetid
    Speed = 700, -- Percentage, 100 = default Rush speed
    DelayTime = 10, -- Time before starting cycles (seconds)
    HeightOffset = 0,
    CanKill = true,
    KillRange = 40,
    BackwardsMovement = false,
    BreakLights = true,
    FlickerLights = {
        true, -- Enabled/Disabled
        19, -- Time (seconds)
    },
    Cycles = {
        Min = 9,
        Max = 9,
        WaitTime = 0,
    },
    CamShake = {
        true, -- Enabled/Disabled
        {3.5, 20, 0.1, 1}, -- Shake values (don't change if you don't know)
        120, -- Shake start distance (from Entity to you)
    },
    Jumpscare = {
        False, -- Enabled/Disabled
        {
            Image1 = "rbxassetid://10483855823", -- Image1 url
            Image2 = "rbxassetid://10483999903", -- Image2 url
            Shake = true,
            Sound1 = {
                5263560896, -- SoundId
                { Volume = 1.0 }, -- Sound properties
            },
            Sound2 = {
                5263560896, -- SoundId
                { Volume = 1.0 }, -- Sound properties
            },
            Flashing = {
                true, -- Enabled/Disabled
                Color3.fromRGB(255, 255, 255), -- Color
            },
            Tease = {
                false, -- Enabled/Disabled
                Min = 1,
                Max = 3,
            },
        },
    },
    CustomDialog = {"OH MY GOD AMBUSH IS SCREAMING", "LET ME GET YOUR BALLS!"}, -- Custom death message
})


-----[[  Debug -=- Advanced  ]]-----
entityTable.Debug.OnEntitySpawned = function()
    print("Entity has spawned:", entityTable)
    
end

entityTable.Debug.OnEntityDespawned = function()
    print("Entity has despawned:", entityTable)
end

entityTable.Debug.OnEntityStartMoving = function()
    print("Entity has started moving:", entityTable)
end

entityTable.Debug.OnEntityFinishedRebound = function()
    print("Entity has finished rebound:", entityTable)
end

entityTable.Debug.OnEntityEnteredRoom = function(room)
    print("Entity:", entityTable, "has entered room:", room)
end

entityTable.Debug.OnLookAtEntity = function()
    print("Player has looked at entity:", entityTable)
end

entityTable.Debug.OnDeath = function()
    warn("Player has died.")
end
------------------------------------


-- Run the created entity
Spawner.runEntity(entityTable)
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddLabel("Custom Entity")

Tab:AddButton({
	Name = "Angry Munci",
	Callback = function()
      		local Spawner = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors%20Entity%20Spawner/Source.lua"))()


-- Create entity
local entityTable = Spawner.createEntity({
    CustomName = "Angry Munci", -- Custom name of your entity
    Model = "rbxassetid://12304983631", -- Can be GitHub file or rbxassetid
    Speed = 322, -- Percentage, 100 = default Rush speed
    DelayTime = 29, -- Time before starting cycles (seconds)
    HeightOffset = 0,
    CanKill = true,
    KillRange = 50,
    BackwardsMovement = false,
    BreakLights = true,
    FlickerLights = {
        true, -- Enabled/Disabled
        57, -- Time (seconds)
    },
    Cycles = {
        Min = 40,
        Max = 40,
        WaitTime = 0,
    },
    CamShake = {
        true, -- Enabled/Disabled
        {1000, 1000, 1000, 1000}, -- Shake values (don't change if you don't know)
        120, -- Shake start distance (from Entity to you)
    },
    Jumpscare = {
        false, -- Enabled/Disabled
        {
            Image1 = "rbxassetid://10483855823", -- Image1 url
            Image2 = "rbxassetid://10483999903", -- Image2 url
            Shake = true,
            Sound1 = {
                5263560896, -- SoundId
                { Volume = 1.0 }, -- Sound properties
            },
            Sound2 = {
                5263560896, -- SoundId
                { Volume = 1.0 }, -- Sound properties
            },
            Flashing = {
                true, -- Enabled/Disabled
                Color3.fromRGB(255, 255, 255), -- Color
            },
            Tease = {
                false, -- Enabled/Disabled
                Min = 1,
                Max = 3,
            },
        },
    },
    CustomDialog = {"You dead to Angry munci...", "This one is special!"}, -- Custom death message
})


-----[[  Debug -=- Advanced  ]]-----
entityTable.Debug.OnEntitySpawned = function()
    print("Entity has spawned:", entityTable)
    
end

entityTable.Debug.OnEntityDespawned = function()
    print("Entity has despawned:", entityTable)
end

entityTable.Debug.OnEntityStartMoving = function()
    print("Entity has started moving:", entityTable)
end

entityTable.Debug.OnEntityFinishedRebound = function()
    print("Entity has finished rebound:", entityTable)
end

entityTable.Debug.OnEntityEnteredRoom = function(room)
    print("Entity:", entityTable, "has entered room:", room)
end

entityTable.Debug.OnLookAtEntity = function()
    print("Player has looked at entity:", entityTable)
end

entityTable.Debug.OnDeath = function()
    warn("Player has died.")
end
------------------------------------


-- Run the created entity
Spawner.runEntity(entityTable)
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddLabel("Custom Entity")

Tab:AddButton({
	Name = "LSplash",
	Callback = function()
      		---====== Define spawner ======---

local Spawner = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Entity%20Spawner/Source.lua"))()

---====== Create entity ======---

local entity = Spawner.createEntity({
    CustomName = "LSplash",
    Model = "rbxassetid/12305018197", -- Your entity's model url here ("rbxassetid/12305018197" or GitHub raw url)
    Speed = 666,
    MoveDelay = 2,
    HeightOffset = 0,
    CanKill = true,
    KillRange = 50,
    SpawnInFront = false,
    ShatterLights = false,
    FlickerLights = {
        Enabled = true,
        Duration = 7
    },
    Cycles = {
        Min = 1,
        Max = 999,
        WaitTime = 2,
    },
    CamShake = {
        Enabled = true,
        Values = {9.9, 90, 0.9, 9},
        Range = 100
    },
    ResistCrucifix = true,
    BreakCrucifix = false,
    DeathMessage = {"Hey Idiot.. You CHEATED ON ME HOW DARE YOU!", "AND YES I ASKED LSPLASH TO BAN YOU!", "You Piece A Sh#t", "I Hate You..", "GO AWAY"},
    IsCuriousLight = true
})

---====== Debug ======---

entity.Debug.OnEntitySpawned = function()
    print("Entity has spawned")
end

entity.Debug.OnEntityDespawned = function()
    print("Entity has despawned")
end

entity.Debug.OnEntityStartMoving = function()
    print("Entity started moving")
end

entity.Debug.OnEntityFinishedRebound = function()
    print("Entity finished rebound")
end

entity.Debug.OnEntityEnteredRoom = function(room)
    print("Entity entered room:", room)
end

entity.Debug.OnLookAtEntity = function()
    print("Player looking at entity")
end

entity.Debug.OnDeath = function()
    print("Player has died")
end

--[[
    NOTE: By overwriting 'OnUseCrucifix', the default crucifixion will be ignored and this function will be called instead

    entity.Debug.OnUseCrucifix = function()
        print("Custom crucifixion script here")
    end
]]--

---====== Run entity ======---

Spawner.runEntity(entity)
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddLabel("Custom Entity")

Tab:AddButton({
	Name = "Greed",
	Callback = function()
      		---====== Define spawner ======---

local Spawner = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Entity%20Spawner/Source.lua"))()

---====== Create entity ======---

local entity = Spawner.createEntity({
    CustomName = "Greed",
    Model = "rbxassetid/12304888795", -- Your entity's model url here ("rbxassetid/12304888795" or GitHub raw url)
    Speed = 327,
    MoveDelay = 2,
    HeightOffset = 0,
    CanKill = true,
    KillRange = 50,
    SpawnInFront = false,
    ShatterLights = true,
    FlickerLights = {
        Enabled = true,
        Duration = 2
    },
    Cycles = {
        Min = 1,
        Max = 6,
        WaitTime = 2
    },
    CamShake = {
        Enabled = true,
        Values = {4.4, 10, 0.8, 3},
        Range = 100
    },
    ResistCrucifix = false,
    BreakCrucifix = true,
    DeathMessage = {"You Died To Greed", "Hes So Fast..", "SURVIVE HIM!"},
    IsCuriousLight = false
})

---====== Debug ======---

entity.Debug.OnEntitySpawned = function()
    print("Entity has spawned")
end

entity.Debug.OnEntityDespawned = function()
    print("Entity has despawned")
end

entity.Debug.OnEntityStartMoving = function()
    print("Entity started moving")
end

entity.Debug.OnEntityFinishedRebound = function()
    print("Entity finished rebound")
end

entity.Debug.OnEntityEnteredRoom = function(room)
    print("Entity entered room:", room)
end

entity.Debug.OnLookAtEntity = function()
    print("Player looking at entity")
end

entity.Debug.OnDeath = function()
    print("Player has died")
end

--[[
    NOTE: By overwriting 'OnUseCrucifix', the default crucifixion will be ignored and this function will be called instead

    entity.Debug.OnUseCrucifix = function()
        print("Custom crucifixion script here")
    end
]]--

---====== Run entity ======---

Spawner.runEntity(entity)
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

local Tab = Window:MakeTab({
	Name = ">Doors But Kawaii<",
	Icon = "rbxassetid://7734068321",
	PremiumOnly = false
})

--[[
Name = <string> - The name of the tab.
Icon = <string> - The icon of the tab.
PremiumOnly = <bool> - Makes the tab accessible to Sirus Premium users only.
]]

Tab:AddLabel("Kawaii Entity")

Tab:AddButton({
	Name = "A-60!",
	Callback = function()
      		---====== Define spawner ======---

local Spawner = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Entity%20Spawner/Source.lua"))()

---====== Create entity ======---

local entity = Spawner.createEntity({
    CustomName = "Kawaii A-60",
    Model = "rbxassetid/12500028509", -- Your entity's model url here ("rbxassetid/12500028509" or GitHub raw url)
    Speed = 275,
    MoveDelay = 2,
    HeightOffset = 0,
    CanKill = true,
    KillRange = 50,
    SpawnInFront = false,
    ShatterLights = false,
    FlickerLights = {
        Enabled = true,
        Duration = 2
    },
    Cycles = {
        Min = 1,
        Max = 1,
        WaitTime = 2,
    },
    CamShake = {
        Enabled = true,
        Values = {4.9, 10, 0.2, 6},
        Range = 100
    },
    ResistCrucifix = true,
    BreakCrucifix = false,
    DeathMessage = {"Here he is.."},
    IsCuriousLight = true
})

---====== Debug ======---

entity.Debug.OnEntitySpawned = function()
    print("Entity has spawned")
end

entity.Debug.OnEntityDespawned = function()
    print("Entity has despawned")
end

entity.Debug.OnEntityStartMoving = function()
    print("Entity started moving")
end

entity.Debug.OnEntityFinishedRebound = function()
    print("Entity finished rebound")
end

entity.Debug.OnEntityEnteredRoom = function(room)
    print("Entity entered room:", room)
end

entity.Debug.OnLookAtEntity = function()
    print("Player looking at entity")
end

entity.Debug.OnDeath = function()
    print("Player has died")
end

--[[
    NOTE: By overwriting 'OnUseCrucifix', the default crucifixion will be ignored and this function will be called instead

    entity.Debug.OnUseCrucifix = function()
        print("Custom crucifixion script here")
    end
]]--

---====== Run entity ======---

Spawner.runEntity(entity)
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddLabel("Kawaii Entity")

Tab:AddButton({
	Name = "Ambushy",
	Callback = function()
      		---====== Define spawner ======---

local Spawner = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Entity%20Spawner/Source.lua"))()

---====== Create entity ======---

local entity = Spawner.createEntity({
    CustomName = "Kawaii Ambush",
    Model = "rbxassetid/11734849372", -- Your entity's model url here ("rbxassetid/11734849372" or GitHub raw url)
    Speed = 200,
    MoveDelay = 2,
    HeightOffset = 0,
    CanKill = true,
    KillRange = 50,
    SpawnInFront = false,
    ShatterLights = true,
    FlickerLights = {
        Enabled = true,
        Duration = 1
    },
    Cycles = {
        Min = 1,
        Max = 1,
        Delay = 2
    },
    CamShake = {
        Enabled = true,
        Values = {3.6, 10, 0.2, 4},
        Range = 100
    },
    ResistCrucifix = false,
    BreakCrucifix = true,
    DeathMessage = {"Oh... Hello.", "I'm surprised you found this place.", "It's pretty tedious just to get here, last time i checked.", "Anyways, what'd you die to?.", "Oh... the green one. im not too sure on what I would call it...", "Well, it usually attacks like Ambush, so...", "...you could just call it Ambush. I dont know.", "Anyways, i hope you don't mind trying again. it would be helpful."},
    IsCuriousLight = false
})

---====== Debug ======---

entity.Debug.OnEntitySpawned = function()
    print("Entity has spawned")
end

entity.Debug.OnEntityDespawned = function()
    print("Entity has despawned")
end

entity.Debug.OnEntityStartMoving = function()
    print("Entity started moving")
end

entity.Debug.OnEntityFinishedRebound = function()
    print("Entity finished rebound")
end

entity.Debug.OnEntityEnteredRoom = function(room)
    print("Entity entered room:", room)
end

entity.Debug.OnLookAtEntity = function()
    print("Player looking at entity")
end

entity.Debug.OnDeath = function()
    print("Player has died")
end

--[[
    NOTE: By overwriting 'OnUseCrucifix', the default crucifixion will be ignored and this function will be called instead

    entity.Debug.OnUseCrucifix = function()
        print("Custom crucifixion script here")
    end
]]--

---====== Run entity ======---

Spawner.runEntity(entity)
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

local Tab = Window:MakeTab({
	Name = "~Birthday Mode~",
	Icon = "rbxassetid://7734068321",
	PremiumOnly = false
})

--[[
Name = <string> - The name of the tab.
Icon = <string> - The icon of the tab.
PremiumOnly = <bool> - Makes the tab accessible to Sirus Premium users only.
]]

Tab:AddLabel("Plamen6789Rocks Entitys")

Tab:AddButton({
	Name = "Birthday Ambush",
	Callback = function()
	---====== Define spawner ======---

local Spawner = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Entity%20Spawner/Source.lua"))()

---====== Create entity ======---

local entity = Spawner.createEntity({
    CustomName = "Birthday Ambush",
    Model = "https://github.com/plamen6789/CustomDoorsMonsters/blob/main/BirthdayAmbush.rbxm?raw=true", -- Your entity's model url here ("rbxassetid://1234567890" or GitHub raw url)-<Depth>-
    Speed = 289,
    MoveDelay = 2,
    HeightOffset = 0,
    CanKill = true,
    KillRange = 40,
    SpawnInFront = false,
    ShatterLights = true,
    FlickerLights = {
        Enabled = true,
        Duration = 1.5
    },
    Cycles = {
        Min = 1,
        Max = 6,
        WaitTime = 1
    },
    CamShake = {
        Enabled = true,
        Values = {1.6, 20, 0.1, 1},
        Range = 100
    },
    ResistCrucifix = false,
    BreakCrucifix = true,
    DeathMessage = {"You Died To Birthday Ambush", "Use What You Learned From Ambush!"},
    IsCuriousLight = false
})

---====== Debug ======---

entity.Debug.OnEntitySpawned = function()
    print("Entity has spawned")
end

entity.Debug.OnEntityDespawned = function()
    print("Entity has despawned")
end

entity.Debug.OnEntityStartMoving = function()
    print("Entity started moving")
end

entity.Debug.OnEntityFinishedRebound = function()
    print("Entity finished rebound")
end

entity.Debug.OnEntityEnteredRoom = function(room)
    print("Entity entered room:", room)
end

entity.Debug.OnLookAtEntity = function()
    print("Player looking at entity")
end

entity.Debug.OnDeath = function()
    print("Player has died")
end

--[[
    NOTE: By overwriting 'OnUseCrucifix', the default crucifixion will be ignored and this function will be called instead

    entity.Debug.OnUseCrucifix = function()
        print("Custom crucifixion script here")
    end
]]--

---====== Run entity ======---

Spawner.runEntity(entity)
	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]
	

Tab:AddLabel("Plamen6789Rocks Entitys")

Tab:AddButton({
	Name = "Birthday Rush",
	Callback = function()
      		local Spawner = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors%20Entity%20Spawner/Source.lua"))()


-- Create entity
local entityTable = Spawner.createEntity({
    CustomName = "Birthday Rush", -- Custom name of your entity
    Model = "https://github.com/plamen6789/CustomDoorsMonsters/blob/main/BirthdayRush.rbxm?raw=true", -- Can be GitHub file or rbxassetid
    Speed = 100, -- Percentage, 100 = default Rush speed
    DelayTime = 2, -- Time before starting cycles (seconds)
    HeightOffset = 0,
    CanKill = true,
    KillRange = 30,
    BackwardsMovement = false,
    BreakLights = true,
    FlickerLights = {
        true, -- Enabled/Disabled
        1, -- Time (seconds)
    },
    Cycles = {
        Min = 1,
        Max = 1,
        WaitTime = 2,
    },
    CamShake = {
        true, -- Enabled/Disabled
        {3.5, 20, 0.1, 1}, -- Shake values (don't change if you don't know)
        100, -- Shake start distance (from Entity to you)
    },
    Jumpscare = {
        true, -- Enabled/Disabled
        {
            Image1 = "rbxassetid://10483855823", -- Image1 url
            Image2 = "rbxassetid://10483999903", -- Image2 url
            Shake = true,
            Sound1 = {
                10483790459, -- SoundId
                { Volume = 0.5 }, -- Sound properties
            },
            Sound2 = {
                10483837590, -- SoundId
                { Volume = 0.5 }, -- Sound properties
            },
            Flashing = {
                true, -- Enabled/Disabled
                Color3.fromRGB(255, 255, 255), -- Color
            },
            Tease = {
                true, -- Enabled/Disabled
                Min = 1,
                Max = 3,
            },
        },
    },
    CustomDialog = {"You Died To Rush", "Just Hide! ... I-", "Im Sorry I Yell Sus...", "Forgive Me Please.."}, -- Custom death message
})


-----[[  Debug -=- Advanced  ]]-----
entityTable.Debug.OnEntitySpawned = function()
    print("Entity has spawned:", entityTable)
end

entityTable.Debug.OnEntityDespawned = function()
    print("Entity has despawned:", entityTable)
end

entityTable.Debug.OnEntityStartMoving = function()
    print("Entity has started moving:", entityTable)
end

entityTable.Debug.OnEntityFinishedRebound = function()
    print("Entity has finished rebound:", entityTable)
end

entityTable.Debug.OnEntityEnteredRoom = function(room)
    print("Entity:", entityTable, "has entered room:", room)
end

entityTable.Debug.OnLookAtEntity = function()
    print("Player has looked at entity:", entityTable)
end

entityTable.Debug.OnDeath = function()
    warn("Player has died.")
end
------------------------------------


-- Run the created entity
Spawner.runEntity(entityTable)
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

local Tab = Window:MakeTab({
	Name = "♧Replace Doors Themes♧",
	Icon = "rbxassetid://7734068321",
	PremiumOnly = false
})

--[[
Name = <string> - The name of the tab.
Icon = <string> - The icon of the tab.
PremiumOnly = <bool> - Makes the tab accessible to Sirus Premium users only.
]]

local Section = Tab:AddSection({
	Name = "Replace Musics!"
})

--[[
Name = <string> - The name of the section.
]]

Tab:AddLabel("Enraged Figure Intense Music")

Tab:AddButton({
	Name = "Enraged",
	Callback = function()
      		function ReplaceAudGit(GithubSnd,SoundName) 
         local url=GithubSnd 
         if not isfile(SoundName..".mp3") then 
                 writefile(SoundName..".mp3", game:HttpGet(url)) 
         end 
         return (getcustomasset or getsynasset)(SoundName..".mp3") 
 end 
 workspace.Ambience_FigureIntense.SoundId = ReplaceAudGit("https://github.com/beanzjsjwj/Seek/blob/main/Enraged.mp3?raw=true","Enraged") 
 workspace.Ambience_FigureIntense.Volume = 0.6
 workspace.Ambience_FigureIntense.Loop = true
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddLabel("Perfect Seek Remix")

Tab:AddButton({
	Name = "Ambience_Epic_Seek_Remix",
	Callback = function()
      		function ReplaceAudGit(GithubSnd,SoundName) 
         local url=GithubSnd 
         if not isfile(SoundName..".mp3") then 
                 writefile(SoundName..".mp3", game:HttpGet(url)) 
         end 
         return (getcustomasset or getsynasset)(SoundName..".mp3") 
 end 
 workspace.Ambience_Seek.SoundId = ReplaceAudGit("https://github.com/beanzjsjwj/Seek/blob/main/Remixo.mp3?raw=true","Remixo") 
 workspace.Ambience_Seek.Volume = 1.4
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddLabel("Mrbeast X Here I Come")

Tab:AddButton({
	Name = "MrBeast",
	Callback = function()
      		function ReplaceAudGit(GithubSnd,SoundName) 
         local url=GithubSnd 
         if not isfile(SoundName..".mp3") then 
                 writefile(SoundName..".mp3", game:HttpGet(url)) 
         end 
         return (getcustomasset or getsynasset)(SoundName..".mp3") 
 end 
 workspace.Ambience_Seek.SoundId = ReplaceAudGit("https://github.com/beanzjsjwj/Seek/blob/main/BEAST.mp3?raw=true","BEAST") 
 workspace.Ambience_Seek.Volume = 1.5
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddLabel("Figure Remix")

Tab:AddButton({
	Name = "Ambience_Figure_Remix",
	Callback = function()
      		function ReplaceAudGit(GithubSnd,SoundName) 
         local url=GithubSnd 
         if not isfile(SoundName..".mp3") then 
                 writefile(SoundName..".mp3", game:HttpGet(url)) 
         end 
         return (getcustomasset or getsynasset)(SoundName..".mp3") 
 end 
 workspace.Ambience_Figure.SoundId = ReplaceAudGit("https://github.com/beanzjsjwj/Seek/blob/main/FigureRemix.mp3?raw=true","FigureRemix") 
 workspace.Ambience_Figure.Volume = 0.5
 workspace.Ambience_Figure.Loop = true
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddLabel("Seek Hardcore")

Tab:AddButton({
	Name = "Ambience_Seek_Hardcore",
	Callback = function()
      		function ReplaceAudGit(GithubSnd,SoundName) 
         local url=GithubSnd 
         if not isfile(SoundName..".mp3") then 
                 writefile(SoundName..".mp3", game:HttpGet(url)) 
         end 
         return (getcustomasset or getsynasset)(SoundName..".mp3") 
 end 
 workspace.Ambience_Seek.SoundId = ReplaceAudGit("https://github.com/beanzjsjwj/Seek/blob/main/Hardcore.mp3?raw=true","Hardcore") 
 workspace.Ambience_Seek.Volume = 1
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddLabel("Figure Piano")

Tab:AddButton({
	Name = "Ambience_Figure_Piano",
	Callback = function()
      		function ReplaceAudGit(GithubSnd,SoundName) 
         local url=GithubSnd 
         if not isfile(SoundName..".mp3") then 
                 writefile(SoundName..".mp3", game:HttpGet(url)) 
         end 
         return (getcustomasset or getsynasset)(SoundName..".mp3") 
 end 
 workspace.Ambience_Figure.SoundId = ReplaceAudGit("https://github.com/beanzjsjwj/Seek/blob/main/UnhingedPiano.mp3?raw=true","UnhingedPiano") 
 workspace.Ambience_Figure.Volume = 0.5
 workspace.Ambience_Figure.Loop = true
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddLabel("Seek Altelz Vex Remix")

Tab:AddButton({
	Name = "Ambience_Seek_Vex_Remix",
	Callback = function()
      		function ReplaceAudGit(GithubSnd,SoundName) 
         local url=GithubSnd 
         if not isfile(SoundName..".mp3") then 
                 writefile(SoundName..".mp3", game:HttpGet(url)) 
         end 
         return (getcustomasset or getsynasset)(SoundName..".mp3") 
 end 
 workspace.Ambience_Seek.SoundId = ReplaceAudGit("https://github.com/beanzjsjwj/Seek/blob/main/Here.mp3?raw=true","Here") 
 workspace.Ambience_Seek.Volume = 1
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddLabel("Seek Down")

Tab:AddButton({
	Name = "Boogie",
	Callback = function()
      		function ReplaceAudGit(GithubSnd,SoundName) 
         local url=GithubSnd 
         if not isfile(SoundName..".mp3") then 
                 writefile(SoundName..".mp3", game:HttpGet(url)) 
         end 
         return (getcustomasset or getsynasset)(SoundName..".mp3") 
 end 
 workspace.Ambience_Seek.SoundId = ReplaceAudGit("https://github.com/beanzjsjwj/Seek/blob/main/I.mp3?raw=true","I") 
 workspace.Ambience_Seek.Volume = 1
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

local Tab = Window:MakeTab({
	Name = "¤♡Ui's♡¤",
	Icon = "rbxassetid://7734068321",
	PremiumOnly = false
})

--[[
Name = <string> - The name of the tab.
Icon = <string> - The icon of the tab.
PremiumOnly = <bool> - Makes the tab accessible to Sirus Premium users only.
]]

Tab:AddLabel("Plamen6789Rocks Spawner")

Tab:AddButton({
	Name = "Plamen6789",
	Callback = function()
      		loadstring(game:HttpGet('https://raw.githubusercontent.com/plamen6789/UtilitiesHub/main/UtilitiesGUI'))()
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

local Tab = Window:MakeTab({
	Name = "◇♤Achievement's♤◇",
	Icon = "rbxassetid://4483345998",
	PremiumOnly = false
})

--[[
Name = <string> - The name of the tab.
Icon = <string> - The icon of the tab.
PremiumOnly = <bool> - Makes the tab accessible to Sirus Premium users only.
]]

local Section = Tab:AddSection({
	Name = "What"
})

--[[
Name = <string> - The name of the section.
]]

Tab:AddLabel("Crucifix Hub")

Tab:AddButton({
	Name = "CrucifixHub",
	Callback = function()
      		--[[

    NOTE: Keep in mind that these are "fake" achievements and will NOT give you an in-game badge.

]]--

local Achievements = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Custom%20Achievements/Source.lua"))()

-- Creates and displays your custom achievement
Achievements.Get({
    Title = "「✝」-~{Crucifix-Hub}~-",
    Desc = "Thanks For Using 「✝」-~{Crucifix-Hub}~-!",
    Reason = "~{☆Made By Sus☆}~",
    Image = "https://tr.rbxcdn.com/8fd01aed533ef0d236d7a9cbaf0caada/420/420/Decal/Png",
})
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddLabel("Room's Achievement")

Tab:AddButton({
	Name = "A-1000",
	Callback = function()
      		--[[

    NOTE: Keep in mind that these are "fake" achievements and will NOT give you an in-game badge.

]]--

local Achievements = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Custom%20Achievements/Source.lua"))()

-- Creates and displays your custom achievement
Achievements.Get({
    Title = "A-1000",
    Desc = "I can't feel my legs.",
    Reason = "Reach the end of The Rooms.",
    Image = "https://tr.rbxcdn.com/8158121295ae7f33f97fb8e692f91450/420/420/Image/Png",
})
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddButton({
	Name = "Detour",
	Callback = function()
      		--[[

    NOTE: Keep in mind that these are "fake" achievements and will NOT give you an in-game badge.

]]--

local Achievements = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Custom%20Achievements/Source.lua"))()

-- Creates and displays your custom achievement
Achievements.Get({
    Title = "Detour",
    Desc = "I think I'm lost.",
    Reason = "Enter The Rooms",
    Image = "https://tr.rbxcdn.com/5a2eb1b397fdaf63ab4c8e42c11bd608/420/420/Image/Png",
})
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddButton({
	Name = "Back On Track",
	Callback = function()
      		--[[

    NOTE: Keep in mind that these are "fake" achievements and will NOT give you an in-game badge.

]]--

local Achievements = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Custom%20Achievements/Source.lua"))()

-- Creates and displays your custom achievement
Achievements.Get({
    Title = "Back On Track",
    Desc = "That sucked... but hey, check out this cool flashlight!",
    Reason = "Escape The Rooms.",
    Image = "https://tr.rbxcdn.com/629c5de83a9a7e603ab60822fd023b1d/420/420/Image/Png",
})
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddLabel("General Achievements")

Tab:AddButton({
	Name = "Welcome",
	Callback = function()
      		--[[

    NOTE: Keep in mind that these are "fake" achievements and will NOT give you an in-game badge.

]]--

local Achievements = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Custom%20Achievements/Source.lua"))()

-- Creates and displays your custom achievement
Achievements.Get({
    Title = "Welcome",
    Desc = "Enjoy your stay!",
    Reason = "Join for the first time.",
    Image = "https://static.wikia.nocookie.net/doors-game/images/9/90/Welcome_Badge.png",
})
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddButton({
	Name = "Welcome Back",
	Callback = function()
      		--[[

    NOTE: Keep in mind that these are "fake" achievements and will NOT give you an in-game badge.

]]--

local Achievements = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Custom%20Achievements/Source.lua"))()

-- Creates and displays your custom achievement
Achievements.Get({
    Title = "Welcome Back",
    Desc = "Are you enjoying your stay?",
    Reason = "Join another day..",
    Image = "https://static.wikia.nocookie.net/doors-game/images/6/64/Welcome_Back_Badge.png",
})
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddButton({
	Name = "Buddy System",
	Callback = function()
      		--[[

    NOTE: Keep in mind that these are "fake" achievements and will NOT give you an in-game badge.

]]--

local Achievements = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Custom%20Achievements/Source.lua"))()

-- Creates and displays your custom achievement
Achievements.Get({
    Title = "Buddy System",
    Desc = "Everything's better with friends!",
    Reason = "Play a run with a friend..",
    Image = "https://static.wikia.nocookie.net/doors-game/images/b/b3/Buddy_System_Badge.png",
})
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddButton({
	Name = "Back From The Dead",
	Callback = function()
      		--[[

    NOTE: Keep in mind that these are "fake" achievements and will NOT give you an in-game badge.

]]--

local Achievements = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Custom%20Achievements/Source.lua"))()

-- Creates and displays your custom achievement
Achievements.Get({
    Title = "Back From The Dead",
    Desc = "I have unfinished business.",
    Reason = "Revive yourself.",
    Image = "https://static.wikia.nocookie.net/doors-game/images/3/35/Back_From_The_Dead_Badge.png",
})
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddButton({
	Name = "Join The Group",
	Callback = function()
      		--[[

    NOTE: Keep in mind that these are "fake" achievements and will NOT give you an in-game badge.

]]--

local Achievements = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Custom%20Achievements/Source.lua"))()

-- Creates and displays your custom achievement
Achievements.Get({
    Title = "Join The Group",
    Desc = "You're one with the cool kids now!",
    Reason = "Join the LSPLASH group (and get a free revive)!",
    Image = "https://static.wikia.nocookie.net/doors-game/images/b/b4/Join_The_Group_Badge.png",
})
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddButton({
	Name = "Betrayal",
	Callback = function()
      		--[[

    NOTE: Keep in mind that these are "fake" achievements and will NOT give you an in-game badge.

]]--

local Achievements = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Custom%20Achievements/Source.lua"))()

-- Creates and displays your custom achievement
Achievements.Get({
    Title = "Betrayal",
    Desc = "Dibs!",
    Reason = "Steal a hiding spot from someone right before they die.",
    Image = "https://static.wikia.nocookie.net/doors-game/images/4/48/Betrayal_Badge.png",
})
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddButton({
	Name = "In Plain Sight",
	Callback = function()
      		--[[

    NOTE: Keep in mind that these are "fake" achievements and will NOT give you an in-game badge.

]]--

local Achievements = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Custom%20Achievements/Source.lua"))()

-- Creates and displays your custom achievement
Achievements.Get({
    Title = "In Plain Sight",
    Desc = "Quick, behind that conveniently shaped lamp!",
    Reason = "Hide from an entity by staying out of sight.",
    Image = "https://static.wikia.nocookie.net/doors-game/images/3/3a/In_Plain_Sight_Badge.png",
})
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddButton({
	Name = "Interconnected",
	Callback = function()
      		--[[

    NOTE: Keep in mind that these are "fake" achievements and will NOT give you an in-game badge.

]]--

local Achievements = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Custom%20Achievements/Source.lua"))()

-- Creates and displays your custom achievement
Achievements.Get({
    Title = "Interconnected",
    Desc = "Enjoy, future me!",
    Reason = "Preserve a item in a Rift.",
    Image = "https://static.wikia.nocookie.net/doors-game/images/2/21/Interconnected_Badge.png",
})
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddButton({
	Name = "Supporting Small Businesses",
	Callback = function()
      		--[[

    NOTE: Keep in mind that these are "fake" achievements and will NOT give you an in-game badge.

]]--

local Achievements = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Custom%20Achievements/Source.lua"))()

-- Creates and displays your custom achievement
Achievements.Get({
    Title = "Supporting Small Businesses",
    Desc = "Thank you, come again!",
    Reason = "Purchase an item from Jeff",
    Image = "https://static.wikia.nocookie.net/doors-game/images/a/a7/Supporting_Small_Businesses_Badge.png",
})
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddButton({
	Name = "Pls Donate",
	Callback = function()
      		--[[

    NOTE: Keep in mind that these are "fake" achievements and will NOT give you an in-game badge.

]]--

local Achievements = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Custom%20Achievements/Source.lua"))()

-- Creates and displays your custom achievement
Achievements.Get({
    Title = "Pls Donate",
    Desc = "Now I can go to college!",
    Reason = "Fill Jeff's tip jar.",
    Image = "https://static.wikia.nocookie.net/doors-game/images/2/23/Pls_Donate_Badge.png",
})
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddButton({
	Name = "Herb Of Veridis",
	Callback = function()
      		--[[

    NOTE: Keep in mind that these are "fake" achievements and will NOT give you an in-game badge.

]]--

local Achievements = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Custom%20Achievements/Source.lua"))()

-- Creates and displays your custom achievement
Achievements.Get({
    Title = "Herb Of Veridis",
    Desc = "My heart beats stronger...",
    Reason = "Find and use a green herb.",
    Image = "https://static.wikia.nocookie.net/doors-game/images/8/89/Herb_Of_Viridis_Badge.png",
})
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddLabel("Entities")

Tab:AddButton({
	Name = "Out Of My Way",
	Callback = function()
      		--[[

    NOTE: Keep in mind that these are "fake" achievements and will NOT give you an in-game badge.

]]--

local Achievements = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Custom%20Achievements/Source.lua"))()

-- Creates and displays your custom achievement
Achievements.Get({
    Title = "Out Of My Way",
    Desc = "I'm walkin' here!",
    Reason = "Successfully survive Rush.",
    Image = "https://static.wikia.nocookie.net/doors-game/images/a/af/Out_Of_My_Way_Badge.png",
})
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddButton({
	Name = "Look At Me",
	Callback = function()
      		--[[

    NOTE: Keep in mind that these are "fake" achievements and will NOT give you an in-game badge.

]]--

local Achievements = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Custom%20Achievements/Source.lua"))()

-- Creates and displays your custom achievement
Achievements.Get({
    Title = "Look At Me",
    Desc = "Last chance to look at me.",
    Reason = "Survive the Eyes.",
    Image = "https://static.wikia.nocookie.net/doors-game/images/6/66/Look_at_Me_Badge.png",
})
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddButton({
	Name = "Two Steps Forward...",
	Callback = function()
      		--[[

    NOTE: Keep in mind that these are "fake" achievements and will NOT give you an in-game badge.

]]--

local Achievements = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Custom%20Achievements/Source.lua"))()

-- Creates and displays your custom achievement
Achievements.Get({
    Title = "Two Steps Forward...",
    Desc = "...and one step back.",
    Reason = "Survive Halt.",
    Image = "https://static.wikia.nocookie.net/doors-game/images/7/76/Two_Steps_Forward_Badge.png",
})
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddButton({
	Name = "You Can Run",
	Callback = function()
      		--[[

    NOTE: Keep in mind that these are "fake" achievements and will NOT give you an in-game badge.

]]--

local Achievements = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Custom%20Achievements/Source.lua"))()

-- Creates and displays your custom achievement
Achievements.Get({
    Title = "You Can Run",
    Desc = "But you can't um... um... uhh...",
    Reason = "Successfully run from Seek.",
    Image = "https://static.wikia.nocookie.net/doors-game/images/5/56/You_Can_Run_Badge.png",
})
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddButton({
	Name = "Eviction Notice",
	Callback = function()
      		--[[

    NOTE: Keep in mind that these are "fake" achievements and will NOT give you an in-game badge.

]]--

local Achievements = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Custom%20Achievements/Source.lua"))()

-- Creates and displays your custom achievement
Achievements.Get({
    Title = "Eviction Notice",
    Desc = "And stay out!",
    Reason = "Get kicked out of a hiding spot by Hide.",
    Image = "https://static.wikia.nocookie.net/doors-game/images/9/96/Eviction_Notice_Badge.png",
})
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddButton({
	Name = "Meet Jack",
	Callback = function()
      		--[[

    NOTE: Keep in mind that these are "fake" achievements and will NOT give you an in-game badge.

]]--

local Achievements = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Custom%20Achievements/Source.lua"))()

-- Creates and displays your custom achievement
Achievements.Get({
    Title = "Meet Jack",
    Desc = "Ooga booga!",
    Reason = "Encounter Jack.",
    Image = "https://static.wikia.nocookie.net/doors-game/images/9/98/Meet_Jack_Badge.png",
})
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddButton({
	Name = "Meet Timothy",
	Callback = function()
      		--[[

    NOTE: Keep in mind that these are "fake" achievements and will NOT give you an in-game badge.

]]--

local Achievements = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Custom%20Achievements/Source.lua"))()

-- Creates and displays your custom achievement
Achievements.Get({
    Title = "Meet Timothy",
    Desc = "Hello there little spider!",
    Reason = "Encounter Timothy while looting.",
    Image = "https://static.wikia.nocookie.net/doors-game/images/f/f9/Meet_Timothy_Badge.png",
})
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddButton({
	Name = "Rebound",
	Callback = function()
      		--[[

    NOTE: Keep in mind that these are "fake" achievements and will NOT give you an in-game badge.

]]--

local Achievements = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Custom%20Achievements/Source.lua"))()

-- Creates and displays your custom achievement
Achievements.Get({
    Title = "Rebound",
    Desc = "Coming back for seconds!",
    Reason = "Survive Ambush.",
    Image = "https://static.wikia.nocookie.net/doors-game/images/6/60/Rebound_Badge.png",
})
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddButton({
	Name = "I See You",
	Callback = function()
      		--[[

    NOTE: Keep in mind that these are "fake" achievements and will NOT give you an in-game badge.

]]--

local Achievements = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Custom%20Achievements/Source.lua"))()

-- Creates and displays your custom achievement
Achievements.Get({
    Title = "I See You",
    Desc = "Peek-a-boo!",
    Reason = "Dodge Screech's attack.",
    Image = "https://static.wikia.nocookie.net/doors-game/images/c/c7/I_See_You_Badge.png",
})
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddButton({
	Name = "Error",
	Callback = function()
      		--[[

    NOTE: Keep in mind that these are "fake" achievements and will NOT give you an in-game badge.

]]--

local Achievements = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Custom%20Achievements/Source.lua"))()

-- Creates and displays your custom achievement
Achievements.Get({
    Title = "Error",
    Desc = "Well that just happened.",
    Reason = "Encounter the Glitch.",
    Image = "https://static.wikia.nocookie.net/doors-game/images/9/95/Error_Badge_V2.png",
})
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddButton({
	Name = "Wrong Room",
	Callback = function()
      		--[[

    NOTE: Keep in mind that these are "fake" achievements and will NOT give you an in-game badge.

]]--

local Achievements = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Custom%20Achievements/Source.lua"))()

-- Creates and displays your custom achievement
Achievements.Get({
    Title = "Wrong Room",
    Desc = "Well this is awkward...",
    Reason = "Get duped by Dupe.",
    Image = "https://static.wikia.nocookie.net/doors-game/images/9/98/Wrong_Room_Badge.png",
})
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddButton({
	Name = "It Stares Back",
	Callback = function()
      		--[[

    NOTE: Keep in mind that these are "fake" achievements and will NOT give you an in-game badge.

]]--

local Achievements = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Custom%20Achievements/Source.lua"))()

-- Creates and displays your custom achievement
Achievements.Get({
    Title = "It Stares Back",
    Desc = "Guys, wait up!!!",
    Reason = "Encounter the Void.",
    Image = "https://static.wikia.nocookie.net/doors-game/images/3/3d/It_Stares_Back_Badge.png",
})
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddButton({
	Name = "Dead Of Night",
	Callback = function()
      		--[[

    NOTE: Keep in mind that these are "fake" achievements and will NOT give you an in-game badge.

]]--

local Achievements = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Custom%20Achievements/Source.lua"))()

-- Creates and displays your custom achievement
Achievements.Get({
    Title = "Dead Of Night",
    Desc = "Hmm.. It's Late..",
    Reason = "Encounter Dread At 12am or 12pm.",
    Image = "https://upload.wikimedia.org/wikipedia/commons/thumb/3/3d/Clock_12-00.svg/768px-Clock_12-00.svg.png",
})
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddLabel("Crucifix Achievements")

Tab:AddButton({
	Name = "Evil Be Gone",
	Callback = function()
      		--[[

    NOTE: Keep in mind that these are "fake" achievements and will NOT give you an in-game badge.

]]--

local Achievements = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Custom%20Achievements/Source.lua"))()

-- Creates and displays your custom achievement
Achievements.Get({
    Title = "Evil Be Gone",
    Desc = "I banish thee!",
    Reason = "Use a Crucifix successfully.",
    Image = "https://static.wikia.nocookie.net/doors-game/images/b/b5/Evil_Be_Gone_Badge.png",
})
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddButton({
	Name = "Stay Out Of My Way",
	Callback = function()
      		--[[

    NOTE: Keep in mind that these are "fake" achievements and will NOT give you an in-game badge.

]]--

local Achievements = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Custom%20Achievements/Source.lua"))()

-- Creates and displays your custom achievement
Achievements.Get({
    Title = "Stay Out Of My Way",
    Desc = "You're not that guy, pal.",
    Reason = "Use a Crucifix against Rush.",
    Image = "https://static.wikia.nocookie.net/doors-game/images/2/24/Stay_Out_Of_My_Way_Badge.png",
})
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddButton({
	Name = "Unbound",
	Callback = function()
      		--[[

    NOTE: Keep in mind that these are "fake" achievements and will NOT give you an in-game badge.

]]--

local Achievements = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Custom%20Achievements/Source.lua"))()

-- Creates and displays your custom achievement
Achievements.Get({
    Title = "Unbound",
    Desc = "Shut up, thanks.",
    Reason = "Use a Crucifix against Ambush.",
    Image = "https://static.wikia.nocookie.net/doors-game/images/1/1f/Unbound_Badge.png",
})
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddButton({
	Name = "Take A Breather",
	Callback = function()
      		--[[

    NOTE: Keep in mind that these are "fake" achievements and will NOT give you an in-game badge.

]]--

local Achievements = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Custom%20Achievements/Source.lua"))()

-- Creates and displays your custom achievement
Achievements.Get({
    Title = "Take A Breather",
    Desc = "Sit down for a moment.",
    Reason = "Use a Crucifix against Seek.",
    Image = "https://static.wikia.nocookie.net/doors-game/images/d/d3/Take_A_Breather_Badge.png",
})
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddButton({
	Name = "All Figured Out",
	Callback = function()
      		--[[

    NOTE: Keep in mind that these are "fake" achievements and will NOT give you an in-game badge.

]]--

local Achievements = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Custom%20Achievements/Source.lua"))()

-- Creates and displays your custom achievement
Achievements.Get({
    Title = "All Figured Out",
    Desc = "I figured that wouldn't work for long...",
    Reason = "Use a Crucifix against Figure.",
    Image = "https://static.wikia.nocookie.net/doors-game/images/f/f1/All_Figured_Out_Badge.png",
})
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddButton({
	Name = "You Can't See Me",
	Callback = function()
      		--[[

    NOTE: Keep in mind that these are "fake" achievements and will NOT give you an in-game badge.

]]--

local Achievements = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Custom%20Achievements/Source.lua"))()

-- Creates and displays your custom achievement
Achievements.Get({
    Title = "You Can't See Me",
    Desc = "That probably wasn't the best use of one of those.",
    Reason = "Use a Crucifix against Eyes.",
    Image = "https://static.wikia.nocookie.net/doors-game/images/a/a5/You_Can%27t_See_Me_Badge.png",
})
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddButton({
	Name = "I Hate You",
	Callback = function()
      		--[[

    NOTE: Keep in mind that these are "fake" achievements and will NOT give you an in-game badge.

]]--

local Achievements = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Custom%20Achievements/Source.lua"))()

-- Creates and displays your custom achievement
Achievements.Get({
    Title = "I Hate You",
    Desc = "Could you not be your self for five minutes?!",
    Reason = "Use a Crucifix against Screech.",
    Image = "https://static.wikia.nocookie.net/doors-game/images/b/ba/I_Hate_You_Badge.png",
})
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]


Tab:AddButton({
	Name = "Two Steps Ahead",
	Callback = function()
      		--[[

    NOTE: Keep in mind that these are "fake" achievements and will NOT give you an in-game badge.

]]--

local Achievements = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Custom%20Achievements/Source.lua"))()

-- Creates and displays your custom achievement
Achievements.Get({
    Title = "Two Steps Ahead",
    Desc = "I don't feel like dealing with you.",
    Reason = "Use a Crucifix against Halt.",
    Image = "https://static.wikia.nocookie.net/doors-game/images/2/26/Two_Steps_Ahead_Badge.png",
})
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddButton({
	Name = "Outwitted",
	Callback = function()
      		--[[

    NOTE: Keep in mind that these are "fake" achievements and will NOT give you an in-game badge.

]]--

local Achievements = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Custom%20Achievements/Source.lua"))()

-- Creates and displays your custom achievement
Achievements.Get({
    Title = "Outwitted",
    Desc = "The duper becomes... the dupee.",
    Reason = "Use a Crucifix against Dupe.",
    Image = "https://static.wikia.nocookie.net/doors-game/images/e/e7/Outwitted_Badge.png",
})
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddLabel("Floor Achievements")

Tab:AddButton({
	Name = "Sshh!",
	Callback = function()
      		--[[

    NOTE: Keep in mind that these are "fake" achievements and will NOT give you an in-game badge.

]]--

local Achievements = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Custom%20Achievements/Source.lua"))()

-- Creates and displays your custom achievement
Achievements.Get({
    Title = "Sshh!",
    Desc = "Careful not make a sound!",
    Reason = "Escape from the Library in the Hotel.",
    Image = "https://static.wikia.nocookie.net/doors-game/images/f/f3/Sshh_Badge.png",
})
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddButton({
	Name = "Expert Technician",
	Callback = function()
      		--[[

    NOTE: Keep in mind that these are "fake" achievements and will NOT give you an in-game badge.

]]--

local Achievements = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Custom%20Achievements/Source.lua"))()

-- Creates and displays your custom achievement
Achievements.Get({
    Title = "Expert Technician",
    Desc = "I'm the one for the job.",
    Reason = "Complete the Hotel's breaker puzzle in under a minute without any errors.",
    Image = "https://static.wikia.nocookie.net/doors-game/images/c/c9/Expert_Technician_Badge.png",
})
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddButton({
	Name = "Rock Bottom",
	Callback = function()
      		--[[

    NOTE: Keep in mind that these are "fake" achievements and will NOT give you an in-game badge.

]]--

local Achievements = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Custom%20Achievements/Source.lua"))()

-- Creates and displays your custom achievement
Achievements.Get({
    Title = "Rock Bottom",
    Desc = "This is just the beginning.",
    Reason = "Escape the Hotel.",
    Image = "https://static.wikia.nocookie.net/doors-game/images/5/5a/Rock_Bottom_Badge.png",
})
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddLabel("Milestones Achievement's")

Tab:AddButton({
	Name = "One Of Many",
	Callback = function()
      		--[[

    NOTE: Keep in mind that these are "fake" achievements and will NOT give you an in-game badge.

]]--

local Achievements = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Custom%20Achievements/Source.lua"))()

-- Creates and displays your custom achievement
Achievements.Get({
    Title = "One Of Many",
    Desc = "You're just getting started.",
    Reason = "Encounter your first death.",
    Image = "https://static.wikia.nocookie.net/doors-game/images/c/cb/One_Of_Many_Badge.png",
})
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddButton({
	Name = "Ten Of Many",
	Callback = function()
      		--[[

    NOTE: Keep in mind that these are "fake" achievements and will NOT give you an in-game badge.

]]--

local Achievements = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Custom%20Achievements/Source.lua"))()

-- Creates and displays your custom achievement
Achievements.Get({
    Title = "Ten Of Many",
    Desc = "You'll get used to it.",
    Reason = "Encounter your tenth death.",
    Image = "https://static.wikia.nocookie.net/doors-game/images/e/e2/Ten_Of_Many_Badge.png",
})
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddButton({
	Name = "Hundred Of Many",
	Callback = function()
      		--[[

    NOTE: Keep in mind that these are "fake" achievements and will NOT give you an in-game badge.

]]--

local Achievements = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Custom%20Achievements/Source.lua"))()

-- Creates and displays your custom achievement
Achievements.Get({
    Title = "Hundred Of Many",
    Desc = "That's more i like it!",
    Reason = "Encounter your hundredth death.",
    Image = "https://static.wikia.nocookie.net/doors-game/images/c/c4/Hundred_Of_Many_Badge.png",
})
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddLabel("Old")

Tab:AddButton({
	Name = "Out of many Rebounds",
	Callback = function()
      		--[[

    NOTE: Keep in mind that these are "fake" achievements and will NOT give you an in-game badge.

]]--

local Achievements = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Custom%20Achievements/Source.lua"))()

-- Creates and displays your custom achievement
Achievements.Get({
    Title = "Out of many Rebounds",
    Desc = "Might Come back or not",
    Reason = "Encounter and survive the rare Entity called A-60.",
    Image = "https://us-tuna-sounds-images.voicemod.net/c9eb7eb1-46fd-4040-8677-8b485fdd4026-1677311198231.png",
})
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddLabel("Hardcore")

Tab:AddButton({
	Name = "Shocker",
	Callback = function()
      		--[[

    NOTE: Keep in mind that these are "fake" achievements and will NOT give you an in-game badge.

]]--

local Achievements = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Custom%20Achievements/Source.lua"))()

-- Creates and displays your custom achievement
Achievements.Get({
    Title = "Shocking Experience",
    Desc = "Look at me.",
    Reason = "Encounter Shocker.",
    Image = "https://static.wikia.nocookie.net/0e8fb5ca-a8e7-436a-a05a-0c2bd5bdff87",
})
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddButton({
	Name = "Ripper",
	Callback = function()
      		--[[

    NOTE: Keep in mind that these are "fake" achievements and will NOT give you an in-game badge.

]]--

local Achievements = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Custom%20Achievements/Source.lua"))()

-- Creates and displays your custom achievement
Achievements.Get({
    Title = "Torn Apart",
    Desc = "Dont leave to early..",
    Reason = "Encounter Ripper.",
    Image = "https://i.ytimg.com/vi/91KfDbfD8oM/mqdefault.jpg",
})
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddButton({
	Name = "Deer God",
	Callback = function()
      		--[[

    NOTE: Keep in mind that these are "fake" achievements and will NOT give you an in-game badge.

]]--

local Achievements = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Custom%20Achievements/Source.lua"))()

-- Creates and displays your custom achievement
Achievements.Get({
    Title = "Last Chance To Look Away",
    Desc = "Why are you running?",
    Reason = "Survive the rare entity called Deer God.",
    Image = "https://2static1.fjcdn.com/comments/The+creature+in+the+picture+reminds+me+about+specimen+8+_eb6aa625637f28e19bc30a46b7aef480.png",
})
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddButton({
	Name = "Button!",
	Callback = function()
      		print("button pressed")
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddLabel("Custom Achievement's")

Tab:AddButton({
	Name = "Guiding Light",
	Callback = function()
      		--[[

    NOTE: Keep in mind that these are "fake" achievements and will NOT give you an in-game badge.

]]--

local Achievements = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Custom%20Achievements/Source.lua"))()

-- Creates and displays your custom achievement
Achievements.Get({
    Title = "Light Of Female.",
    Desc = "Hello...",
    Reason = "Encounter Guiding Light.",
    Image = "https://tr.rbxcdn.com/252c9437fd472f9e46453c0124b95cf9/420/420/Decal/Png",
})
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddButton({
	Name = "Threat",
	Callback = function()
      		--[[

    NOTE: Keep in mind that these are "fake" achievements and will NOT give you an in-game badge.

]]--

local Achievements = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/Utilities/main/Doors/Custom%20Achievements/Source.lua"))()

-- Creates and displays your custom achievement
Achievements.Get({
    Title = "Unthreater",
    Desc = "Be Gone Evil..",
    Reason = "Use a Crucifix against Threat.",
    Image = "https://raw.githubusercontent.com/HollowedOutMods/Doors/main/ThreatCrux.png",
})
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

local Tab = Window:MakeTab({
	Name = "☆->}Doors Modes{<-☆",
	Icon = "rbxassetid://4483345998",
	PremiumOnly = false
})

--[[
Name = <string> - The name of the tab.
Icon = <string> - The icon of the tab.
PremiumOnly = <bool> - Makes the tab accessible to Sirus Premium users only.
]]

local Section = Tab:AddSection({
	Name = "Modes By Owners!"
})

--[[
Name = <string> - The name of the section.
]]


Tab:AddLabel("Oof's Scripts")

Tab:AddButton({
	Name = "Impossible Mode",
	Callback = function()
      		loadstring(game:HttpGet('https://raw.githubusercontent.com/Ukazix/impossible-mode/main/Protected_79.lua.txt'))()
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddButton({
	Name = "Mayhem Mode",
	Callback = function()
      		loadstring(game:HttpGet('https://github.com/HollowedOutMods/MayhemMode/blob/main/loader.lua?raw=true'))()
  	end    
})

--[[
Name = <string> - The name of the button.
Callback = <function> - The function of the button.
]]

Tab:AddLabel("Get Hardcore Mode Yourself!")